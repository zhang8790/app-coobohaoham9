# 来电有喜 · 系统诊断优化交付与上线清单

> 针对张林提供的《系统诊断报告》，拍板"全部优化"（除 P5 律师合规外的开发项全做）。
> 代码全部完成，四块优化冷重建 **零编译错误** 通过。

## ✅ 已交付（代码 + 构建）

### P4 扫描结论层 — `src/pages/food/food-scan/index.tsx`
- 新增结论卡：聚合已有 `grade`(S/A/C/D) + `ageSuitability.infantSafe` + `allergens.detected`
- 一句话输出：**能买吗 / 能给孩子吃吗 / 适合谁**，文案过 `shieldCopy`，保留免责声明
- 纯聚合既有字段，零臆造

### P2 食养去医疗化 — 诊断词→食养偏好框架
- `src/utils/food-therapy/aux-remind.ts`：体质偏寒/寒凉/温热/易上火 → 口感口味偏好
- `src/pages/mine/health-profile/index.tsx`：标题「我的体质档案」→「我的食养偏好档案」
- `src/pages/index/index.tsx`：3 处「体质/身体状况」→「食养偏好」
- 内部匹配 key 不动（防回归）；用户自填标签原值保留（App 不下诊断）

### P1 首屏定位 — `src/pages/index/index.tsx`
- hero 后加「扫码查安全」入口承诺 banner（第一视觉锚点）
- 其余功能收为二阶入口，不破坏现有 tab/handler

### P3 文章社交 — 最小社交闭环
- `src/db/types.ts`：`ArticleFavorite` / `AuthorFollow`
- `src/db/api.ts`：`toggleArticleFavorite` / `isArticleFavorited` / `toggleAuthorFollow` / `isFollowingAuthor`
- `src/pages/content/article-detail/index.tsx`：假收藏→真落库 + 新增关注作者 + 状态拉取
- `src/pages/user/index.tsx`：「文章收藏」「我的关注」入口
- 新建 `src/pages/mine/article-favorites/index.tsx` + `src/pages/mine/followed-authors/index.tsx` + 注册路由

### 🔧 顺带修复（早于本轮的隐蔽坑）
- `knowledge-atlas` 页面注册路径（`pages/food` 子包）与文件位置（顶层 `pages/`）及跳转路径三处不一致 → 整包构建失败
- 已 `git mv` 到 `src/pages/food/knowledge-atlas/` 并同步跳转；批量校验 56 个子包页面 0 缺失

### 构建验证
- P4+P2：25.04s ✅ ｜ P1：25.17s ✅ ｜ P3（修路径后）：26.72s ✅
- `dist/` 三处新页面（article-favorites / followed-authors / food/knowledge-atlas）三件套就位

---

## ⏳ 待上线（需你在工具链执行，我无法安全盲推）

| # | 动作 | 原因 |
|---|---|---|
| 1 | 微信开发者工具上传 `dist/` | 含批次录入页 + P1–P4 + knowledge-atlas 修复 |
| 2 | 生产 Supabase 执行 `supabase/migrations/00216_article_social.sql` | 文章收藏/关注表+RLS；未执行则 P3 真机写库失败 |
| 3 | git 提交 / 推送 | 全部改动累积未提交，等「归档/推送」 |
| 4 | P5 律师合规评估 | 健康豆代币定性 + 二级分销，非开发项 |

> ⚠️ 本地未链接 Supabase 项目（无 `.supabase` 链接 + 143 个迁移不能盲推），故第 2 步按惯例由你在 dashboard/链接后定向执行 `00216` 单文件，避免误推全部迁移。

## 重点提示
- 健康豆 1:1 锚定 + 抵扣的**代币定性风险**、二级分销**被定性传销风险** → 必须找懂微信生态律师评估（协议页已写「不可提现/兑现金/二级转让」作防线，但代码救不了政策定性）。
- 食养文案已按合规铁律去医疗化；后续新增用户可见文案务必过 `shield.ts` 的 `shieldCopy()`。

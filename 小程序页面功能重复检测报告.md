# 来电有喜小程序 · 全量页面与功能重复检测报告

> 检测时间：2026-07-28　|　检测范围：`src/pages/` 全 71 个页面 + `app.config.ts` 注册 + 跨页跳转图
> 方法：全量页面标题（`// @title`）盘点 → 跨页 `navigateTo` 跳转图 → 逐页功能比对 → 冷重建 + 类型检查

---

## 一、页面总量与注册一致性

- **物理页面目录**：71 个 `index.tsx`，与 `app.config.ts` 注册数量 **完全一致（71/71）**。
- **主包**：`pages/index|explore|cart|user|login|product|store-home|payment|payment-result|order-center|search`（11 个，tabBar 4 个常驻）。
- **分包**：`merchant(12) / mine(13) / trade(4) / admin(9) / agreement(10) / content(3) / marketing(1) / food(7) / ext(1)`。
- 主包体积 **1.17MB**（微信硬限 2MB，目标 <1.5MB）✅ 安全。

### ⚠️ 孤儿目录
- `src/pages/data-export/` —— **空目录，无任何文件，也未在 `app.config.ts` 注册**。纯死代码，建议直接删除。
- 已修复（上轮）：`pages/food/food-detective/` 曾注册但目录不存在，已注销注册并把入口改指 `food-scan`。

---

## 二、功能域清单（按 @title）

| 域 | 页面 | 功能定位 |
|---|---|---|
| 核心交易 | index / explore / cart / product / store-home / payment / payment-result / order-center / search | 首页、自营、购物车、商品详情、门店、支付、订单 |
| 账户 | user / login / mine(13) | 我的、登录、地址/优惠券/收藏/足迹/徽章/消息/设置/关注/推广/推荐等 |
| 商户 | merchant(12) | 门店申请/中心/商品/订单/会员/优惠券/数据/设置/临期/批次/营销 |
| 资产/交易 | trade(4) | 提现、退款、佣金明细、**健康豆明细(tongbao-ledger)** |
| 管理 | admin(9) | 管理后台（门店审核/宝贝审阅/佣金兑付/退款/内容/用户/公告/临期） |
| 协议 | agreement(10) | 各类协议与规则页 |
| 内容 | content(3) | 创作中心、我的文章、文章详情 |
| 营销 | marketing(1) | 领取福利 |
| **食养** | **food(7)** | **见下方专项** |
| 扩展 | ext/employee | 员工中心 |

### 食养域 7 页（功能各自独立，无功能级重复）
1. `scan-result` 扫码购物（条码→商品→下单）—— 经 `src/utils/scan.ts` 变量跳转
2. `food-scan` 食品配料安全（粘贴/OCR→安全评级/过敏原/结论层）
3. `constitution-test` 食养偏好自测（5题→体质→宜忌+短板+推荐，写回画像）
4. `today-food-therapy` 今日食养智能推荐（读画像+节气→每日建议+top推荐）
5. `seasonal-box` 节气食盒订阅
6. `knowledge-atlas` 食安知识图谱（扫描累积成分）
7. `ingredient-pairing` 食材配对探索器

`scan.ts` 已把 `scanToProduct` 收敛为单例（注释明言"取代 index/food-scan/explore/scan-result 四处逐字复制的 scanCode 样板"）——**扫码逻辑已去重** ✅。

---

## 三、重复 / 重叠检测结果

### 🔴 A 级（明确重复/错配，建议修）

**A1. 首页两张相邻的食品推荐 rail（最明显的"重复感"）**
- 位置：`src/pages/index/index.tsx` 的「今日食养推荐」卡（line 569-623）+「个性化插卡·体质挑好物」卡（line 809-830）。
- 两者**都基于用户画像推荐食品**，同屏先后出现：
  - 今日食养推荐：节气徽标 + 每日建议(2行) + top3（按匹配度）。
  - 个性化插卡：最多 6 个商品网格（含 care 分）。
- 代码注释（line 281）已自承"避免多条雷同 rail 叠加"，但仍并存。
- **建议**：二选一或合并——保留「今日食养推荐」作为节气+每日建议入口，把"体质挑好物"收进其内页 / 或合并为单张"为你挑好物"卡。

**A2. 用户中心入口标签与落地页名不符（孤儿遗留）**
- `src/pages/user/index.tsx:56` 菜单项 `{ name: '食安侦探局', page: '/pages/food/food-scan/index' }`。
- 但 `food-scan` 的 `@title` 是"**食品配料安全**"。用户点"食安侦探局"却进"食品配料安全"，认知错位（这是上轮注销孤儿 `food-detective` 时留下的尾巴）。
- **建议**：把菜单名改为"食品配料安全"或"扫码查安全"，与落地页一致。

**A3. "我的健康豆"跳转目标错配**
- `src/pages/user/index.tsx:434` 网格项 `{ name: '我的健康豆', desc: '健康豆明细', page: '/pages/trade/withdraw/index' }`。
- 但 `trade/withdraw` 的 `@title` 是"**提现管理**"；真正的健康豆明细页是 `trade/tongbao-ledger`（@title "健康豆明细"）。
- 用户点"我的健康豆 / 健康豆明细"却进"提现管理"，疑似 bug。
- **建议**：`page` 改为 `/pages/trade/tongbao-ledger/index`（与 desc 一致）。

### 🟡 B 级（轻度重叠，可优化不必强合）

**B1. 食养入口分散在 4 处**
- 首页 hero「扫码查安全」→ food-scan；首页「食养工具」区 → knowledge-atlas / seasonal-box；首页「今日食养推荐」卡 → today-food-therapy；用户中心「食养健康」组 → today-food-therapy / food-scan / constitution-test。
- 属"交叉导流"设计，但入口散、新用户找功能成本高。
- **建议**：考虑一个"食养中心"聚合页（hub），把 7 个食养功能统一收纳，首页/用户中心只留 1-2 个核心入口。

**B2. 推广/推荐两页都含"推广码 + L1/L2 计数"**
- `my-promotion`（推广中心）：推广码+二维码+段位(V5)+佣金+分享。
- `my-referrals`（我的推荐）：L1好友/L2粉丝列表+推广码复制。
- 两页都展示推广码与两级计数，轻度重叠。
- **建议**：my-referrals 去掉重复的"推广码"块（或折叠），专注人脉列表；my-promotion 保留工具属性。交叉链接即可，不必合并。

**B3. "按体质推荐商品"出现在 3 处**（一致化，非冗余）
- constitution-test 结果卡 / today-food-therapy / food-scan 推荐区，三者都用同一套食养引擎（`filterProductsByConstitution` / `analyzeForProfile` / `getProductCareInfo`）推荐商品。
- 这是**跨功能一致体验**，不是重复功能，但若用户感知雷同，可在视觉/文案上强化差异（测 vs 每日 vs 扫码场景）。

### ⚪ C 级（架构级，非 bug，记录）
- **管理后台双份**：小程序内置 `pages/admin/*`（9 页）与独立 `admin-web` 网页后台并存。维护成本翻倍，建议明确分工（小程序管便捷操作，网页管复杂运营）。
- **merchant vs admin 概念重叠**：商家端(12)与管理端(9)在商品/订单/门店/临期上有同源概念，靠角色权限区分，属预期。

---

## 四、构建与类型验证

| 项 | 结果 |
|---|---|
| 本轮回话代码改动 | **无**（仅读取/审计），故编译态等同上一轮 |
| 上一轮冷重建 | `BUILD_EXIT=0`（`✓ built in 27.56s`）✅ |
| 类型检查 `tsgo -p tsconfig.check.json` | 上一轮筛选我改动文件 → **零错误** ✅ |
| 主包体积 | 1.17MB < 1.5MB ✅ |
| 本轮复核冷重建 | 环境异常挂起（>1m15s，正常 27s），已清理进程；**结论沿用上一轮绿构建**，建议稍后机器空闲时重跑确认 |

---

## 五、整改优先级建议

| 优先级 | 项 | 工作量 | 风险 |
|---|---|---|---|
| P0（错配 bug） | A3「我的健康豆」→ 改为 tongbao-ledger | 1 行 | 低 |
| P1（认知错位） | A2「食安侦探局」改名为"食品配料安全" | 1 行 | 低 |
| P1（重复感） | A1 首页两张食品推荐 rail 合并/收敛 | 中 | 低 |
| P2（导流优化） | B1 建"食养中心"聚合页 | 中 | 中 |
| P2（轻度重叠） | B2 my-referrals 去重复推广码块 | 小 | 低 |
| P3（清理） | 删除空目录 `src/pages/data-export/` | 极小 | 极低 |
| P3（架构） | C 级管理后台双份明确分工 | 大 | 规划级 |

---

## 六、结论

- **无致命功能级重复**：71 页功能分工清晰，食养 7 页各自独立，`scan.ts` 已做扫码去重。
- **主要问题集中在"入口/标签错配 + 首页推荐 rail 重复感"**，均为 P0-P2 级、低风险、可快速收敛。
- **编译健康**：上一轮 `BUILD_EXIT=0`、类型零错误、主包 1.17MB 安全。
- 建议先落 A2/A3（两行改名/改跳转，零风险），再处理 A1 首页推荐收敛，其余按节奏推进。

# 上线就绪检查报告（2026-07-30）

> 范围：小程序（Taro weapp）、管理端（admin-web）、Supabase 后端（RLS / 分佣 / 食品配料 / Edge Functions）
> 结论：**核心功能与构建全部达标，可进入"上传+部署"阶段**；有 1 项安全加固建议与 1 项待清理项，均非上线阻断。

---

## ✅ 一、达标项（通过）

### 1. 小程序 Taro 构建
- 命令：`mv dist dist.bak.<ts> && taro build --type weapp`
- 结果：`✓ built in 52.23s`，**零编译错误**
- `dist/` 顶层入口齐全：`app.js / app.json / app.wxss / common.js / project.config.json`
- 页面产物含新增页：`food/analysis-result`、`food/food-scan` 等
- ⚠️ 注意：直接 `rm -rf dist` 会被 WorkBuddy safe-delete 拦截（239 文件超阈值、trash 失败），**必须用 `mv dist dist.bak.<ts>` 改名重建**（见下方"必须动作"）。

### 2. 管理端 admin-web 构建
- `tsc -b`：**零类型错误**
- `vite build`：`✓ built in 1.90s`（产物 844KB JS / 1.2KB CSS，仅有 chunk 体积告警，非错误）
- ⚠️ 同样需用 `mv dist dist.bak.<ts>` 绕开 safe-delete 才能成功。

### 3. 去 AI 化（用户端）
- 残留 `i-mdi-*` 已全部清零（仅 `mdiPaths.tsx` 第 5 行保留注释，无影响）
- 修正点：帮助中心 2 个、个人中心 1 处死分支的 `i-mdi-` 判断已改为 emoji / 直出
- 底部导航 `CustomTabBar` 已编译进 `dist/common.js`，渲染 5 项（首页/自营/创作居中凸起/购物车/我的），使用 base64 手绘 SVG（非 AI 图标），页签 path 与 `app.config.ts` 的 4 个 switchTab 路径完全对齐
- 情绪编译 `emotion-compile` Edge Function 已部署（v10），含口语化硬约束

### 4. RLS 策略完整性
- `products`：`rls81_products_merchant_write` + `rls81_products_owner` + read ✓（商家上架/保存失败已修复）
- `product_emotion`：`rls81_product_emotion_merchant_write` + admin + read ✓（情绪工作台保存失败已修复）
- `merchant_applications`：`rls_mapp_owner` ✓
- `commissions` / `orders` / `order_items` 均有 admin + owner/merchant 策略 ✓

### 5. 商品级分佣全链路
- `order_item_commissions`：**305 行**（商品级分佣已落库）
- 触发器 `trg_distribute_commission`：**EXISTS**
- `orders.commission_distributed = true`：**264 单**已分佣
- 退款回冲列 `refund_ratio` / `refunded_at`：**EXISTS**（`refund-order` / `wechat-refund-callback` 已部署 v16/v13，含按商品比例回冲）
- 历史订单补齐（~96 单/120 行）已幂等写入

### 6. 食品配料安全（建立在原 API 上）
- 基础表种子就位：`food_allergens`=8、`food_crowd_triggers`=12、`food_crowd_tips`=13
- `food_analysis_reports` 表存在（0 行，待录入属正常）
- `ingredient-analyze` Edge Function 已部署（v1）
- 前端 `food-safety.ts` + 标准报告页 `food/analysis-result` + 管理端 `FoodSafetyLibs` 均已接入并随构建产出

### 7. 其他已部署 Edge Functions（节选）
`distribute-commission`(v32, 今日 09:33)、`create-order`(v22)、`wechat-payment-callback`(v15)、`ocr-ingredient`(v8)、`expiry-engine`(v4)、`get-wechat-openid`、`send-notification`、`send-redpacket` 等均为 ACTIVE。
- `get_recent_order_feed` RPC：**EXISTS**

---

## ⚠️ 二、风险 / 待办（非阻断）

| # | 项 | 说明 | 建议 |
|---|----|------|------|
| 1 | `order_item_commissions` RLS 关闭 | `relrowsecurity=false` → 任何人（含 anon）可读写该表；当前 admin 读取正常，但存在被注入伪造分佣行的安全风险 | **上线前建议**：启用 RLS 并加 `authenticated` 读策略（admin/merchant 按归属读），写仅限 service_role（EF） |
| 2 | `refud-order`（slug=smart-endpoint）重复函数 | 与 `refund-order` 并存，疑似误部署的副本 | 确认无引用后删除以免混淆 |
| 3 | 小程序未上传 | `dist/` 已编译，但微信后台尚未收到提审包 | 见"必须动作 1" |
| 4 | 管理端未部署 | `admin-web/dist/` 已编译，但托管未更新 | 见"必须动作 2" |
| 5 | `dist.bak.*` 本地堆积 | 多次构建产生的 7 个备份目录（共约 1785 文件） | 上线后手动清理（safe-delete 会拦截批量 rm，需逐目录移回收站） |

---

## 📋 三、上线前必须动作（人工）

1. **小程序提审/发布**：用微信开发者工具打开本仓库，`上传` 已生成的 `dist/` 目录 → 提交审核 → 发布。
2. **管理端部署**：将 `admin-web/dist/` 部署到你的静态托管（如 CloudBase / 对象存储 / Vercel 等）。
   - ⚠️ 当前 `admin-web/.env.local` 的 `VITE_SUPABASE_SERVICE_ROLE_KEY` 为空 → 管理端走 anon+session，**受 RLS 约束**。现有策略已覆盖，但如后续新增写操作需同步补策略（或填入 service_role key 以绕过 RLS）。
3. **（强烈建议）补 `order_item_commissions` RLS 策略**，消除开放写风险。

---

## 四、本次检查顺带修复
- 帮助中心 2 个图标（`i-mdi-share-variant-outline` / `i-mdi-account-circle-outline`）→ emoji `💰` / `👤`
- 个人中心 / 帮助中心两处 `i-mdi-` 死分支判断 → 直出 emoji
- 临时校验 SQL 已留存于 `scripts/_rls_check.sql`、`scripts/_chain_check.sql`（可删）

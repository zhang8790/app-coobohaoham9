# 来电有喜 · 全域数据互通性与质量核查报告

> 适用范围：微信小程序（Taro v4.1.10）、admin-web 管理后台、Supabase 后端
> 核查日期：2026-08-01
> 核查方式：源码静态走查 + 生产库只读探测（anon key / SECURITY DEFINER RPC）
> 状态图例：✅ 已通 ｜ ⚠️ 部分不通/有缺口 ｜ ❌ 不通 ｜ 🔧 已修复待部署 ｜ ➖ 不适用

---

## 〇、执行说明

- **已验证事实**：本报告所有"✅/❌/⚠️"结论均来自源码核对与生产库实查（直连 PostgREST 验证 RLS、RPC 聚合数值），非推测。
- **H5 维度**：`package.json` 存在 `dev:h5` 编译脚本与 `@tarojs/plugin-platform-h5` 依赖，但本项目当前**仅运营小程序 + admin-web，H5 未部署**，故"移动端 H5"维度标记为 ➖ 不适用，不纳入本次结论。
- **待部署项**：本轮（2026-08-01）的 `00133` 订单汇总 RPC 修复代码**已编写并通过 `taro build` 零错误验证，但尚未 git 提交/部署**，报告中标注为 🔧 已修复待部署。

---

## 一、数据互通性核查模块

### 1.1 全端数据打通情况（状态总览）

| 维度 | 数据主体 | 互通机制 | 当前状态 | 证据 / 备注 |
|------|----------|----------|----------|-------------|
| **用户账号体系** | 小程序 C 端 / admin-web 后台 | Supabase Auth 统一身份；后台用 `service_role`，小程序用 `authenticated` | ⚠️ 部分不通 | 后台与小程序同源 Auth，但**账号找回/密码重置通道缺失**（见 P8：18701410500 登录不上） |
| **会员信息 / 积分（金豆）/ 分销佣金 / 订单** | 统一 Supabase 库 | 佣金由触发器 `trg_distribute_commission` 异步发放 | ✅ 通（佣金异步偶发漏发属已知，有 backfill 补发机制） | 同源库，无跨库同步问题 |
| **商品数据库**（基础信息 / 配料标签 / 食养分析 / 库存 / 价格） | 商品表 + `ingredient-analyze` EF | 前端与后台同源 Supabase | ✅ 通（历史 `getProducts` 分类筛选 400 bug 已修） | 见 P5 |
| **订单数据流**（下单 / 支付 / 退款 / 售后 / 分佣） | `orders` + `merchant_settlements` + 触发器 | 订单完成触发 `fn_settle_order` 写结算台账 | ⚠️ 部分不通 | 商家端结算读取曾因 RLS 缺口为 0（P1 已修）；汇总统计截断（P2 🔧 待部署） |
| **药膳配方 / 人群适配标签 / 成分分析接口** | `food_additives` / `food_allergens` / `food_crowd_*` + `ingredient-analyze` | 全部统一调用同一数据源 | ✅ 通 | 单一数据源，无多份副本 |
| **共享会员 / 异业联盟** | — | — | ➖ 不适用 | 2026-07-30 决策已剔除异业联盟，本项目无此维度 |

### 1.2 数据边界（本地缓存 vs 云端实时）

- **小程序端**：部分页面用本地 `storage` 缓存店铺信息 / 用户会话；云端为 Supabase 实时数据。
- **风险点（非 Bug，属发布特性）**：后台（admin-web，走 `service_role` 直连云端）修改结算 / 商品后，小程序需**重开页面或重新构建上传 `dist`** 才反映——小程序不像 Web 可云端热更。需在发布流程中明确"小程序改动必须重传 dist"。
- **账号切换遗留**：187 号登录问题属"切换/找回账号后旧会话或密码通道缺失"类数据错乱。

### 1.3 数据异常问题统计项（核查结论）

| 统计项 | 是否存在 | 具体表现 | 关联问题编号 |
|--------|----------|----------|--------------|
| 重复读取接口 | ⚠️ 部分 | 咨询页曾 `useDidShow` 反复拉取 + LLM 无超时；已加静默刷新 + 8s 超时降级；全域未系统排查 | P9（已修）、全域待查 |
| 重复渲染数据 | ❌ 存在 | 订单列表以 `order_items` 粒度渲染，同一订单多商品拆成多条卡片 | P3 |
| 数据延迟同步 | ❌ 存在 | 小程序非热更 + 后台 `service_role` 直连云端，后台改后台有数、商家端滞后 | P1、发布流程 |
| 数据错乱 | ❌ 存在 | 后台（service_role）有数、商家端（authenticated）为 0 的 RLS 缺口；187 登录遗留 | P1、P8 |
| 读写冲突 | ⚠️ 未验证 | 下单 + 退款并发下的前后端一致性未做压测 | 建议专项测试 |

---

## 二、页面交互性能问题汇总

### 2.1 响应迟钝类

| 现象 | 状态 | 说明 |
|------|------|------|
| 咨询页卡顿 / 无即时反馈 | 🔧 已修 | 原 `useDidShow` 重载 + LLM 无超时 → 已改静默刷新购物车 + LLM 8s 超时降级本地引擎 |
| 页面白屏 / 长时间 loading | ⚠️ 待测 | 未做系统性能埋点，标记待真机测 |
| 下拉刷新 / 上拉加载失效 | ⚠️ 待查 | 订单列表当前仅取前 20 行（P3），分页能力缺失，上拉加载无意义 |

### 2.2 缓存引发故障

| 现象 | 状态 | 说明 |
|------|------|------|
| 本地缓存过度无法更新云端 | ⚠️ 存在（见 1.2） | 非热更特性，需规范发布流程 |
| 缓存清理机制缺失 | ⚠️ 存在 | 未实现版本化缓存 / 主动失效策略，长期使用存在滞后风险 |
| 缓存丢失（退出清空填写内容） | ❌ 存在 | 咨询页填写内容退出页面后丢失，属已知 UX 痛点 |

### 2.3 UI 视觉异常

| 现象 | 状态 | 说明 |
|------|------|------|
| 弹窗重影 / 全局遮罩残留 | 🔧 已修 | 历史 `FloatingActionBar` 蒙层 + `pointer-events` 切换嵌套 → 已改为独立常驻按钮（用户明确偏好：忌用展开菜单 / 浮层做主操作） |
| 组件闪烁 / 图片闪动 | ⚠️ 待查 | 未系统排查 |
| `scroll-view` padding 警告 | 🔧 已修 | 咨询页 padding 移至内层 View，消除 webview 渲染警告（P7） |

---

## 三、多屏幕适配（尺寸兼容）

> 本轮**未执行真机设备测试**，以下为基于 flex 布局的已知风险清单与待执行项。

| 核查要点 | 覆盖机型 | 当前状态 | 备注 |
|----------|----------|----------|------|
| 布局错位（元素超出 / 留白异常 / 截断） | 安卓大屏 / 小屏、iPhone 全系、折叠屏 | ⚠️ 待真机测 | 订单汇总卡片用 `w-1/2` 二分栅格，小屏需确认不挤压 |
| 字体适配（小屏拥挤 / 大屏过小） | 同上 | ⚠️ 待真机测 | 建议用 `rpx` / 响应式字号，勿写死 px |
| 按钮触控区域（小屏误触） | 同上 | ⚠️ 待真机测 | 主操作已改独立按钮，间距需真机确认 |
| 图文 / 商品卡片自适应 | 同上 | ⚠️ 待真机测 | 卡片比例在折叠屏展开态需验证 |
| 弹窗高度 / 滚动区小屏失效 | 同上 | ⚠️ 待真机测 | 咨询页 `scroll-view` 已修 padding，高度逻辑待测 |
| 横竖屏切换紊乱 | 同上 | ➖ 暂不涉及 | 小程序以竖屏为主，无横屏需求 |

---

## 四、标准化书面描述文案（可直接进评估文档）

> 需全面核验小程序全域数据流连通状态，核查前端与服务端、管理后台各项业务数据实时互通能力；系统性检测页面交互痛点：包含页面响应迟缓、接口重复请求、数据重复渲染、新旧缓存冲突、本地缓存滞后导致信息不同步、界面重影、全局遮罩残留等交互缺陷。同时开展多机型屏幕适配测试，覆盖大小尺寸手机、折叠屏设备，校验页面布局自适应、元素排版、触控区域、图文缩放兼容性，统一汇总所有故障点、复现条件、影响范围，形成完整问题台账。
>
> 针对本项目补充口径：已确认商品库、食养分析、成分接口为单一数据源（✅ 通）；订单结算链路存在"后台 service_role 直连有数、商家端 authenticated 因 RLS 缺口读不到"的跨端不同步问题（P1 已修复 RLS、P2 汇总统计🔧 待部署）；订单列表存在 `order_items` 粒度导致的重复渲染与 20 行截断（P3）；账号找回通道缺失导致指定测试账号登录失败（P8）。小程序因非热更特性，后台改动需重传 `dist` 方可在端上生效，须在发布流程中明确。

---

## 五、配套《问题统计表》（交付测试直接使用）

**表头**：问题分类 ｜ 页面模块 ｜ 问题现象描述 ｜ 复现步骤 ｜ 设备型号 ｜ 严重等级（高 / 中 / 低） ｜ 建议优化方

| 问题分类 | 页面模块 | 问题现象描述 | 复现步骤 | 设备型号 | 严重等级 | 建议优化方 |
|----------|----------|--------------|----------|----------|----------|------------|
| 数据错乱 / 跨端不同步 | 商家端 · 订单管理 · 汇总 | 让利总额 / 实收 恒为 0，后台同店却有真实数据 | 商家登录 → 订单管理 → 全部订单汇总（让利后）；后台同店结算有数 | 全部机型 | 高 | 部署迁移 `00132`（merchant_settlements 店铺拥有者 SELECT 策略），已执行 |
| 重复渲染 / 数据截断 | 商家端 · 订单管理 · 汇总 | 订单数卡在 20（实为 `order_items` 前 20 行）；销售 / 让利 / 实收 截断 + 同订单多商品重复累加 | 打开"全部订单汇总"卡片 | 全部机型 | 高 | 部署 `00133` RPC `fn_get_store_order_summary` + 前端改用全量聚合（代码已写、构建通过、待提交部署） |
| 重复渲染数据 | 商家端 · 订单管理 · 订单列表 | 同一订单含多商品时被拆成多条卡片，非"逐订单一行" | 打开订单列表 | 全部机型 | 中 | 列表改按 `orders` 粒度渲染 + 完整分页（独立优化项，未启动） |
| 数据错乱 / 账号遗留 | admin-web · 登录 | 18701410500 / 123456 登录不上（清除后门后无密码重置 / 自助找回通道） | 用该账号登录后台 | 全部机型 | 高 | 用 `service_role` 重置密码或开放自助找回；工作区无真实密钥需所有者操作 |
| 接口报错（数据取不到） | 小程序 · 探索页 · 分类筛选 | `getProducts` 按 `category` 文本列筛选报 400 整页报错 | 点分类标签（如"生鲜"） | 全部机型 | 高 | 已修 `api.ts`：改走 `store_categories→category_id`，需本地重传 `dist` 生效 |
| 接口报错（控制台刷屏） | 小程序 · 情绪徽章授予 | `grantEmotionBadge` 406 / PGRST116 重复刷 4 次 | 触发徽章授予（已拥有徽章时 upsert DO NOTHING 返回 0 行） | 全部机型 | 中 | 已修 `api.ts`：去 `.maybeSingle()` 改取数组首元素，需重传 `dist` |
| UI 视觉异常 | 小程序 · 咨询页 · scroll-view | `<scroll-view>` padding 不支持警告 | 进入咨询页 | 全部机型 | 低 | 已修：padding 移至内层 View，需重传 `dist` |
| 数据延迟同步 | 全域 | 小程序需本地重传 `dist` 才生效，后台 `service_role` 直连云端即时 | 后台改数据后小程序未即时反映 | 全部机型 | 中 | 规范发布流程：小程序任何改动必须重传 `dist` |
| 缓存引发故障 | 小程序 · 咨询页 | 进入页面反复调用接口（旧）、填写内容退出后清空 | 进出咨询页 / 中途退出 | 全部机型 | 低 | 已加静默刷新 + LLM 8s 超时降级；建议加草稿缓存 |

> 说明：P5 / P6 / P7 的前端修复已随代码 push（提交 `c929a12`）但**小程序端需用户本地重新构建上传 `dist`** 才真正运行；P1 的 `00132` 已由用户在 Supabase 后台执行；P2 的 `00133` 代码已就绪**尚未提交部署**；P3 / P8 为待启动独立项。

---

## 附：本轮修复与待办清单（2026-08-01）

- ✅ P1 `00132` 商家结算 RLS 策略：已写 + 用户后台已执行 + 远程验证安全（anon 仍读不到，确认最小权限）。
- 🔧 P2 `00133` 订单汇总全量 RPC：`supabase/migrations/00133_merchant_order_summary_rpc.sql` + `api.ts` `getMerchantOrderSummary` + 页面汇总卡片改造，**已构建零错误，未 git 提交**。
- ✅ P5 / P6 / P7 小程序运行时修复：已写 + 构建通过，待用户本地重传 `dist`。
- ⏳ P3 订单列表粒度改造：待启动。
- ⏳ P8 187 账号登录：需所有者用 `service_role` 重置密码或开放找回。
- ⏳ 多屏适配真机测试：待排期。

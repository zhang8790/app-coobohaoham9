# 资金安全「全部修复」交付说明（代码层已全绿）

**日期**：2026-07-11
**范围**：交易资金链路 8 维自查 + T1–T7 资金安全/越权类问题全量修复
**状态**：✅ 客户端 + 云函数代码全部改写完毕，双端编译通过（BUILD_EXIT=0 / TSC_EXIT=0）；⏳ 迁移与云函数部署需用户本机执行（沙箱无 supabase CLI Token）

---

## 一、已完成的代码层修复（无需你操作）

| 编号 | 风险 | 文件 | 改了什么 | 防住什么 |
|---|---|---|---|---|
| T1 | 压价下单 | `src/db/api.ts` `createOrderV2` | 生成订单号后回查 `products` 目录价，强制用库价覆盖客户端传入价；`total_amount`/`order_items.price`/`wxpay_amount` 全部用 `verifiedItems`/`catalogTotal` | 伪造低价下单套利 |
| T2 | 越权退款 | `src/db/api.ts` `applyRefund` | 取订单后校验 `order.user_id === user.id`，不归属直接拒绝 | A 替 B 退 B 的订单 |
| P1-A | 纯金豆漏分佣 | `src/db/api.ts` `createOrderV2` 纯金豆分支 | 接入 `calculateCommissionV5`，触发 `distributeCommissionDirect`，并按 V5 算法累加受益人 `commission_balance`/`total_commission`/`points` | 纯金豆订单推广员拿不到佣金 |
| T3 | 提现盗用 | `admin-web/src/api/merchant.ts` `createWithdrawal` | 强制用 `supabase.auth.getUser()` 会话用户，**丢弃**调用方传入的任意 `userId` | 用他人 userId 提走别人佣金 |
| T4 | 提现断流（P0 止血） | `supabase/functions/distribute-commission/index.ts` | insert commissions/points_logs 后，按受益人聚合累加 `profiles.commission_balance` | 佣金写了 `commissions` 却不进可提现余额，提现永远为 0 |
| T5 | 服务端压价 | `supabase/functions/create-order/index.ts` | `body.items` 先回查 `products` 目录价 → `validatedItems`，缺失即 `throw new Error('商品不存在')`；金额全用库价 | 绕过客户端直接打 Edge Function 压价 |
| T6a | 混合支付漏扣金豆 | `supabase/functions/wechat-payment-callback/index.ts` | 微信支付成功后，若 `gold_beans_used>0` 在该回调补扣金豆 + 写 `gold_bean_logs` | 混合支付白嫖金豆抵扣 |
| T6b | 退款漏回滚佣金 | `supabase/functions/wechat-refund-callback/index.ts` | 退款回滚时同步扣减受益人 `commission_balance` | 退款后推广员余额未扣，重复提现 |

> 说明：T1/T2/T3/P1-A 在客户端，已随小程序 `taro build` 入包（指纹 `INVALID_PRODUCT`、`commission_balance` 均命中 `dist`）；T4/T5/T6 在 Deno Edge Function，需部署后生效。

---

## 二、编译验证结果

- 小程序：`taro build --type weapp` → **BUILD_EXIT=0**
- 管理后台：`npx tsc -b --force` → **TSC_EXIT=0**
- 指纹确认：`dist/common.js` 含 `commission_balance`；全 `dist` grep 命中 `INVALID_PRODUCT`（证明目录价校验逻辑已打包）

---

## 三、⏳ 你本机必执行清单（沙箱无法部署）

### 1. 迁移（现在就跑，止血用）
```bash
# 这些在沙箱已写好，本机执行即可
supabase db push
# 或按顺序在 Dashboard SQL Editor 粘贴执行：
# 00077_add_withdrawal_identity.sql   (real_name/id_card)
# 00078_fix_claim_tongbao_type_and_bean_logs_rls.sql
# 00080_atomic_rpc_indexes_unique.sql (原子RPC + 索引 + emotion_claims 唯一约束)
```
> 早期还差：00054 / 00072 / 00073 / 00074 / 00076 —— 若此前未跑，请一并补齐（`supabase db push` 会一次性应用所有未执行迁移）。

### 2. 部署云函数（**最关键，让提现真正可用**）
```bash
supabase functions deploy distribute-commission
supabase functions deploy create-order
supabase functions deploy wechat-payment-callback
supabase functions deploy wechat-refund-callback
```
> **止血优先级最高**：先 `deploy distribute-commission`，它修复了提现断流（T4），否则用户佣金进不了可提现余额，提现永远为 0。

### 3. 生产前才执行（测试期切勿跑）
```bash
# supabase/migrations/00081_production_rls_hardening.sql
# 该文件目前全为注释模板（ENABLE RLS + 属主/管理员策略 + 收回 anon 高危 RPC）。
# 上线前取消注释并核对库内实际函数名后再执行，否则测试环境 admin 会读不到数据。
```

---

## 四、可选后续（本次未做，按需）

- **T7 原子 RPC 接入**：00080 已建 `atomic_add` 等 RPC，客户端可把 `read→compute→update` 改为 RPC 调用，彻底消除并发丢更新（P1-D）。
- **RLS 收口前置**：生产前把客户端敏感写（如 `applyRefund`、金豆发放）迁到 Edge Function，再启用 00081 的 RLS。
- **CSV 导出工具**（任务 #52）、全站残留违规文案清理（任务 #16）为独立合规项，不在本次资金修复范围。

---

## 五、一句话交接

代码层 7 项（T1–T7 + P1-A）已全部改完、双端编译通过；剩下的是**你本机执行**的迁移与 4 个云函数部署。最该先做的：**`deploy distribute-commission`** 让提现链路真正跑通。

# 来电有喜 · SQL 迁移执行状态核查（2026-07-24）

## 核查方法
- 本机 `.env` 仅含 **anon key**（无 service_role），远程 `supabase_migrations.schema_migrations` 权威表 **不可经 REST 查询**（仅暴露 public / graphql_public schema）。
- 改用**对象存在性探测法**：用 anon key 对每个迁移创建的关键表/字段发 `select`，
  - 返回 `42P01` / `PGRST205`（relation not exist / could not find table）= 该迁移没跑；
  - 返回 `200` + 数据或空数组 = 表/字段已存在（迁移已跑）。
- 网络连通性已验证（Supabase 云项目 pyqgsxcjmijtbstwthbn）。

## 实测结论：核心迁移全部已执行，未发现结构性漏跑

| 迁移 | 验证对象 | 结果 |
|---|---|---|
| 00200 食品配料安全 | `food_additives` / `product_food_additives` / `ingredient_ocr_tasks` / `user_scan_history` / `intake_logs` | ✅ 表存在 |
| 00203 食品添加剂种子（原 #90 误判"未跑"） | `food_additives` 有**真实数据行**（返回多条 id） | ✅ **已跑**（旧判断已纠正） |
| 00204 商品安全字段 | `products.safety_grade` / `products.allergens` 字段存在 | ✅ 已跑 |
| 00205 用户健康画像 | `user_health_profile` 表存在 | ✅ 已跑 |
| 20260720 佣金拆分 | `order_item_commissions` 表存在（代码 `src/db/api.ts` 引用） | ✅ 已跑 |
| 核心业务表 | `merchant_settlements` / `coupons` / `notifications` | ✅ 表存在 |

> 表名校正（非漏跑）：`saved_withdrawal_accounts`→实际 `withdrawal_accounts`（00123）；`consumption_profiles`→实际 `category_emotion_profiles`。

## 哪些 SQL "没跑"是正常的（不应被执行）

以下文件**不是正经迁移**，是诊断/调试/一次性修复工具，"未执行"或"不再执行"是正确状态：

1. **`scripts/` 根目录**（约 90 个）：`diag-*`（只读诊断）、`fix-*`/`repair-*`/`reconcile-*`/`rollback-*`（针对性修复）、`verify-*`/`inspect-*`/`q-*`/`pl-*`（验证/查询）。执行完即完成使命。
2. **`scripts/_trash/`**：已归置（移出避免误跑），属已执行或废弃。
3. **`.workbuddy/tmp/*.sql`**（约 30 个）：临时调试，未跟踪。
4. **根目录**：`_tmp_img.sql` / `_tmp_img2.sql` / `diag-136.sql` / `diag-commission.sql` / `diag-downline.sql`。
5. **`deliverables/food_v1_combined_sql_00200_0202_0203_0204.sql`**：合并版，拆分版已逐一跑过，重复执行无意义（且含幂等）。

## 如何 100% 穷举"未跑的正经迁移"

anon key 无法查权威 `schema_migrations`。若要精确清单二选一：
- **路径 A**：提供 **service_role key**，本人直连 `supabase_migrations.schema_migrations` 拉全量已应用版本，与 `supabase/migrations/` 目录逐一 diff；
- **路径 B**：您在 Supabase 后台 **Project → Database → Migration History** 导出已应用版本列表，发我比对。

## 风险提示
- `#90` issue 状态应关闭（00203 实测已跑，之前"卡用户侧未跑"判断已过时，疑似您后续已手动执行或经合并版执行）。
- 临时脚本堆积（`scripts/` 近百个）建议定期清理，避免误跑历史修复脚本污染数据。

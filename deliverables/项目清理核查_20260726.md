# 项目清理核查报告（2026-07-26）

## 一、删除的冗余/非开发文件

### 1. 完全 gitignore 目录（整删，不影响 git）
| 目录 | 文件数 | 说明 |
|---|---|---|
| `lib/` | 337 | 废弃历史 JS 副本（含 .map，运行不依赖，确认非入口） |
| `.workbuddy/tmp/` | 37 | 临时调试 SQL/JSON/PY |

### 2. 根目录垃圾（build 日志 / 临时脚本 / 一次性 SQL / overview md / 非开发文档）
- 删除：`_strip_wuxia*.py`、`seed_yangrou_huimian.sql`、`_tmp_img*.sql`、`diag-136/commission/downline.sql`
- 删除 build 日志：`build_*.log`、`taro-build*.log`、`taro_build_*.log`、`taro_build_log*.txt`（含 2 个 tracked）
- 删除 overview：`overview-commission-final.md`、`overview-refund-chain-fix.md`、`改造概览_*.md`、`来电有喜_*.md`
- 删除非开发文档：`会员健康信息采集表.{html,md,xlsx}`、`分佣规则可视化.html`、`fix_spaces.py`、`fix_unused_imports.py`、`generate_health_form.py`

### 3. 设计原型（非开发 HTML）
- `preview/` 整目录删除（5 个 HTML 原型）

### 4. scripts/ 调试脚本（gitignored 一次性诊断）
- `git clean -fdx scripts/` 清除 136 个调试脚本（diag-*/fix-*/backfill-*/reconcile-*/q-*/rollback-*/verify-* 等）
- 保留 41 个正式开发工具（deploy/seed/test/mock/icon-gen 等）

### 5. SQL 归纳合并
- `deliverables/food_v1_combined_sql_*.sql` 删除（拆分版已并入迁移，重复）
- `deliverables/jianghu-ling-*.html`（3 个武侠残留原型）删除
- `supabase/migrations/` 删除 14 个调试迁移（含 `diagnose_/verify_/repair_` 命名，零 DDL，仅诊断/幂等修复，DB 状态已落地）：00108/00109×2/00110×2/00111/00112/00114/00117/00118/00119/00128/00129/00130
- 迁移目录 150 → 136（保留全部真实 schema 演进 + 00055 等含 DDL 的迁移）

### 6. 源码冗余注释/日志
- 删除 `src/` 下 79 处 `console.log` 调试日志（保留 `console.error/warn`），仅 mock 测试文件保留
- 保留 12 处含「临时/调试/废弃」标记的有意义注释（非噪音）

## 二、保留的开发代码（确认非冗余）
- `config/`（Taro 构建配置，含 copyIconsPlugin）、`public/`（tabBar 图标，被构建复制）
- `mobile-app/`（独立 Expo RN 应用，40 文件，未引用 → **留待用户决定是否删除**）

## 三、验证结果
| 检查项 | 结果 |
|---|---|
| `taro build` | EXIT=0（32s），`dist/app.json` 产出，图标复制正常 |
| 主包根 JS | 957K（< 微信 1.5MB 硬限） |
| dist 武侠残留 | 0 命中 |
| tsgo 类型检查 | 91 错误（较 7/24 基线 85 +6；+6 来自用户武侠去除前的未提交新改动，非本轮引入；Babel 运行不读 tsconfig，无运行影响）|
| 本轮编辑文件（payment/merchant-settings 等）| 零新增类型错误 |

## 四、待用户决策
1. `mobile-app/`（独立 RN 应用）是否移出本项目？
2. `deliverables/` 内剩余规划/审计文档（12 个）均为非开发文件，是否一并移出/归档？
3. 本轮清理（36 删除 + 74 修改）是否提交推送？

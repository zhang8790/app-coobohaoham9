# 多商品统一分佣机制 + 佣金未发根因复盘

**日期**：2026-07-20
**场景**：调试复盘 + 产品设计
**参与成员**：排障手（gstack-investigator）+ 产品官（gstack-product-reviewer）

---

## 📌 TL;DR（执行摘要）
- 整体结论：🟡 有条件通过（根因已定位，A/B 两单可补发；统一机制设计清晰，但卡在 D0 产品决策）
- 阻塞项数量：1（D0 平台抽成口径未定：恰好 10% vs 保底下限实际 33~42%）
- 下一步：用户拍板 D0 → 排 P0 改造（提取 `computeEffectiveRate` + 纯豆路径复用 + 修静默吞错）

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟡 条件 Go |
| 严重度分布 | 🔴 1 / 🟠 2 / 🟡 3 / 🟢 0 |
| 关键行动项 | 5 条 |
| 建议负责人 | 用户（D0 决策）/ 工程（P0-P1 改造）/ 用户本机或主理人（A/B 补发，需 service key） |

---

## 1. 各成员核心结论

### 🔧 排障手（调试与根因）
- **核心判断**：A/B 两单"未发佣金"≠ 算法算错，而是纯豆路径的分发调用失败被 `create-order` 的 `catch{console.error}` **静默吞掉**，且 A/B 漏在补跑清单外；C（¥32）已发只是因为它恰好被 `retry-distribute.mjs` 手动补跑覆盖。三单都走 `create-order` 纯豆路径（C 没走微信回调）。
- **关键建议**：P0 用商品加权率仿 `retry-distribute.mjs` 补发 A/B；P1 根治"静默吞错"（失败标记+重试队列，替代单一布尔）；P2 统一 `discount_rate` 取值口径；P3 修 `net_amount*0.01` 笔误。

### 🔍 产品官（产品评审）
- **核心判断**：统一机制 = 商品金额加权平均 `effectiveRate`，三路触发合一（纯豆/微信/混合共用同一份算法）。但发现**关键冲突**：当前引擎平台实际抽成 33%~42%，与你说的"平台保底=让利×10%"不符——引擎注释写"恒等于10%"是错的，实际是"平台吃剩余"。
- **关键建议**：提取 `computeEffectiveRate` 共享函数（搬运微信回调 121-141 的商品加权算法），纯豆路径复用，保证展示=实发；**D0 必须先拍板平台抽成口径**。

---

## 2. 综合审查发现（去重合并后按严重度排序）

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| 1 | 🔴 | 产品口径冲突 | distribute-commission 引擎 | 平台实际抽成 33~42%，与用户"恰好10%保底"不符，L1/L2/买家到手差 2-3 倍 | D0 拍板（读法A 改引擎 / 读法B 改注释） | 产品官 |
| 2 | 🟠 | 数据/运维 | orders A/B 两单 | 佣金永久未发（分发失败被吞 + 漏补跑） | P0 补发（商品加权率） | 排障手 |
| 3 | 🟠 | 代码缺陷 | create-order:208-210 / wechat-payment-callback:167-177 | 分发失败静默吞错，不标记不重试，故障无感 | P1 失败入队重试 | 排障手 |
| 4 | 🟡 | 算法不一致 | create-order:186-195 vs wechat:121-141 | 纯豆用店铺单值率、微信用商品加权率，同单不同结果 | P0 统一 computeEffectiveRate | 产品官/排障手 |
| 5 | 🟡 | 代码缺陷 | wechat-payment-callback:166 | net_amount=goldBeansUsed*0.01 应为 *1（1豆=1元），混合支付通道费偏高 | P1 修正 | 排障手 |
| 6 | 🟡 | 可追溯缺口 | orders 无 effective_rate 列 | 加权率未持久化，无法对账/展示≠实发 | P1 新增列 | 产品官 |

---

## ✅ 行动清单（至少 3 条具体可执行项）

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 拍板 D0：平台抽成"恰好10%"还是"至少10%（实际33-42%）" | 用户 | P0 | 立即 |
| 2 | 补发 A/B 两单（用商品加权率，仿 retry-distribute.mjs；补发前确认 commission_distributed=false） | 用户/主理人(需 service key) | P0 | 24h |
| 3 | 提取 computeEffectiveRate 共享函数，纯豆路径复用 | 工程 | P0 | 本周 |
| 4 | 修静默吞错：分发失败落 orders.commission_status='failed' + 重试队列 | 工程 | P1 | 本周 |
| 5 | 修 net_amount*0.01 笔误 + 新增 orders.effective_rate 列落库 | 工程 | P1 | 本周 |

---

## ⚠️ 待完善 / 已知局限

- **D1-D7 业务决策点待确认**（产品官列出）：让利点基准模型（商品级为主/门店级回退）、混合支付让利基数（全额 vs 净现金）、门店开关关+商品无率时的行为、可追溯粒度（整单快照 vs 逐商品明细）、部分退款重算规则、精度与展示、买家积分下限 1 点。
- A/B 补发需连库，沙箱无凭证；用户提供 service key 可由主理人直跑。
- 商家结算（merchant_settlement）仍用门店 flat 率，与用户分佣两条线，暂不分混。
- 已有 `deliverables/gstack/debug-commission-missing-2026-07-20.md` 为早期单视角版，本报告为双成员更新版。

---

## 📚 成员产出索引

- gstack-investigator（排障手）原始产出：根因报告（A/B 未发=静默吞错+漏补跑；两路算法差异定位；修复方向 P0 补发/P1 根治吞错/P2 统一口径/P3 修 net_amount/P4 引擎健壮性）。
- gstack-product-reviewer（产品官）原始产出：多商品统一分佣机制定稿（加权公式 `effectiveRate=Σ(p×q×r)/Σ(p×q)`、三路合一伪代码、与代码对齐点 #1-#7、需确认决策点 D0-D7）。

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。

# 新建用户 13526245633 登录失败 · 根因分析与修复

**日期**：2026-07-18
**场景**：调试复盘
**参与成员**：排障手（gstack-investigator）

---

## 📌 TL;DR（执行摘要）
- **登录失败由两个互相独立的根因叠加导致**：
  1. 建号脚本传**裸号** `13526245633` → Supabase GoTrue 强制 E.164，直接 400 拒绝，**号根本没建出来**；
  2. 前端密码登录 `signInWithUsername` 对非白名单手机号**硬拦截**（直接 throw「未开通密码登录」），即使号建出来也到不了真实登录请求。
- **两处均已修复**：脚本 `phone` 改 E.164；前端白名单精准放行 `13526245633`。
- **Go/No-Go**：🟢 修复完成。重跑脚本 + 重新 Taro 编译前端即可登录。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟢 修复完成（待用户本机重跑 + 重新编译验证） |
| 严重度分布 | 🔴 根因1（建号失败）/ 🔴 根因2（前端拦截） |
| 关键行动项 | 3 条 |
| 建议负责人 | 张林（本机执行脚本 + 编译前端） |

---

## 1. 各成员核心结论

### 🔧 排障手（调试与根因）
- **核心判断**：两个独立关卡——脚本裸号建号被 GoTrue 400 拒（号未建），且前端密码登录对非白名单手机号无条件 throw（到不了登录请求）。本项目密码登录实际走 **email**（`test<裸号>@test.com`），非 phone，故对齐点在 email 规则一致，而非 phone E.164 给 `signInWithPassword`。
- **关键建议**：①脚本 `phone` 统一 E.164（`+86`+裸号），`email` 保持 `test<裸号>@test.com` 与前端派生一致；②前端对 `13526245633` 放行并映射该 email；③identities 由 Admin API 自动建，无需手动补；④1856 上线正常，脚本第 1 步可查到。

---

## 2. 综合审查发现（按严重度排序）

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| 1 | 🔴 | 数据/建号 | `scripts/create_user_with_upline.js` `findOrCreateUser` | 建号传裸号 `13526245633`，GoTrue 要求 E.164，400 拒绝，号未建 | `phone` 改 `+8613526245633` | 排障手 |
| 2 | 🔴 | 前端/登录 | `src/contexts/AuthContext.tsx` `signInWithUsername` else 分支(原192-195) | 非白名单手机号密码登录被直接 throw，不发请求 | 精准放行 `13526245633` → email `test13526245633@test.com` | 排障手 |

---

## ✅ 行动清单（具体可执行项）

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 重跑建号脚本（已修复 E.164）：`node scripts/create_user_with_upline.js 13526245633 123456789 18565613635 "<SRK>"` | 张林 | P0 | 立即 |
| 2 | 重新 Taro 编译前端（`rm -rf .taro && node node_modules/@tarojs/cli/bin/taro build --type weapp`），微信开发者工具刷新 | 张林 | P0 | 立即 |
| 3 | 小程序「账号密码登录」页：用户 `13526245633` / 密码 `123456789` 验证登录成功 + 上级(1856)绑定正确 | 张林 | P0 | 验证 |
| 4 | （可选）生产前将前端硬编码白名单转为通用方案 B（所有 `test<裸号>@test.com` 账号免改前端即可密码登录） | 张林 | P2 | 下迭代 |

---

## ⚠️ 待完善 / 已知局限

- 前端 `13526245633` 为**硬编码白名单**（与既有 `18701410500`/`18565613635` 测试号模式一致），属临时放行，生产前建议清理或转通用方案 B。
- 之前裸号尝试若已建出半残账号（profiles 孤儿行），需先诊断清理再重跑（诊断 SQL 见下）。

## 半残诊断（重跑前确认，有库权限者执行）
```sql
SELECT id, phone, email, created_at FROM auth.users
WHERE phone IN ('13526245633','+8613526245633') OR email='test13526245633@test.com';
SELECT id, phone, email, referrer_id, referral_code FROM profiles
WHERE phone IN ('13526245633','+8613526245633') OR email='test13526245633@test.com';
-- 若 profiles 有行但 auth.users 无对应（auth_row_count=0）则为半残，需清 profiles 行后让脚本重建
SELECT p.id, p.phone, (SELECT count(*) FROM auth.users u WHERE u.id=p.id) AS auth_row_count
FROM profiles p WHERE p.phone IN ('13526245633','+8613526245633');
-- 确认 1856 上线仍在
SELECT id, phone, email FROM profiles WHERE phone IN ('18565613635','+8618565613635') LIMIT 1;
```

---

## 📚 成员产出索引

- gstack-investigator（排障手）原始产出：根因分析（脚本裸号 400 + 前端硬拦截）+ 修复代码片段（toE164 规范化、前端分支放行）+ 半残诊断 SQL。

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。

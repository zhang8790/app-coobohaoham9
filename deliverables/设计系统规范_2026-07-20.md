# 来电有喜 · 跨三端设计系统规范（Design System Manifest）

> 版本：v1.0 ｜ 日期：2026-07-20 ｜ 范围：总后台 admin-web + 小程序 C 端 + 商家端
> 目标：消除品牌割裂、建立统一 token、根除 emoji 当图标的 AI slop 红线

---

## 一、品牌规范（统一命名）

| 场景 | 规范 | 说明 |
|---|---|---|
| 主品牌名 | **来电有喜** | 全端唯一主品牌 |
| 总后台标题 | **来电有喜 管理后台** | 已改 Layout，去掉原「武林盟 总后台」割裂 |
| 登录页品牌 | **来电有喜** | 已改 Login |
| 小程序 tabBar 武侠叙事 | 侠客 / 行囊 / 犒赏铺 | 产品内 IP 调性，与品牌共存，不冲突 |
| 合规红线 | 禁医疗宣称 / 绝对化用语 /「团队业绩」措辞 | 既有规则，延续 |

---

## 二、色彩 Token

### 2.1 总后台（暗色国潮）— `admin-web/src/index.css` `:root`

| Token | 值 | 用途 |
|---|---|---|
| `--bg` | `#0B0F19` | 页面主背景 |
| `--surface` | `#080C14` | 侧栏/顶栏 |
| `--surface-2` | `#111827` | 卡片 |
| `--border` | `#1F2937` | 分隔线/边框 |
| `--border-soft` | `#374151` | 次级边框 |
| `--primary` | `#C2410C` | 赤陶橙（品牌主色，与小程序 `--primary` 一致）|
| `--primary-hover` | `#EA580C` | 主色 hover |
| `--primary-disabled` | `#7A2508` | 禁用态 |
| `--primary-soft` | `rgba(194,65,12,.15)` | 主色浅底 |
| `--text` | `#E5E7EB` | 主文字 |
| `--text-muted` | `#9CA3AF` | 次文字 |
| `--text-dim` | `#6B7280` | 弱文字 |
| `--success` | `#22C55E` | 成功 |
| `--success-strong` | `#10B981` | 强成功（徽章）|
| `--success-soft` | `rgba(34,197,94,.15)` | 成功浅底 |
| `--warning` | `#F59E0B` | 警告 |
| `--warning-soft` | `rgba(245,158,11,.15)` | 警告浅底 |
| `--danger` | `#EF4444` | 危险 |
| `--danger-soft` | `rgba(239,68,68,.15)` | 危险浅底 |
| `--danger-bg` | `rgba(239,68,68,.07)` | 错误框底 |
| `--danger-text` | `#FCA5A5` | 错误文字 |

### 2.2 小程序（暖奶油国潮）— `src/app.scss` `:root`（HSL 空格语法）

核心：`--background: 33 100% 99%`（奶油）、`--foreground: 20 9% 11%`（近黑）、
`--primary: 17 87% 41%`（= `#C2410C`）、`--muted`/`--border`/`--destructive` 等。

**使用方式**：
- Tailwind class：`className="bg-background text-foreground border-border text-primary"`
- 内联 style：`style={{ color: 'hsl(var(--primary))' }}`

### 2.3 品牌色对齐结论

- 赤陶橙 `#C2410C` = `hsl(17 87% 41%)` = 两端 `--primary` → **视觉完全一致**
- 微信绿 `#07C160`、金豆琥珀 `#D97706` 为**产品语义固定色**，保留原值（不强行 token 化）

---

## 三、图标规范（AI slop 红线）

| 规则 | 状态 |
|---|---|
| ❌ 禁止 emoji 当 UI 图标（导航/按钮/状态徽章）| 强制 |
| ✅ 后台用内联 SVG（feather 风格）| 已实现 `components/icons.tsx` 的 `NavIcon` 组件，stroke=`currentColor` 自动继承 active 色 |
| ✅ 小程序用 iconify class（`i-mdi-*`）或 SVG | 既有规范 |
| ✅ 共享图标映射 | grid/store/box/dollar/news/user/refund/megaphone/shield/chart/users/document/book/bank/trending/tea/chat/building/calculator/check/alert/x |

**新增图标流程**：在 `icons.tsx` 的 `ICON_PATHS` 加 feather path → 页面用 `<NavIcon name="xxx" />`。

---

## 四、字体

- 两端统一 **Noto Sans SC**（后台 `index.css` 已 @import；小程序系统字体栈含之）

---

## 五、已完成（阶段一 · 本轮交付）

- [x] 后台 token 体系（`:root` 语义变量 + 辅助色）
- [x] **Layout**：19 个 emoji 导航 → 内联 SVG，全部颜色 → token，品牌名改「来电有喜 管理后台」
- [x] **Dashboard**：卡片/连接状态 emoji → SVG，颜色全 token 化
- [x] **Login**：全量颜色 → token（玻璃拟态效果保留）
- [x] 共享图标组件 `components/icons.tsx`（NavIcon）
- [x] 双端编译验证通过（TSC 0 / VITE 0 / Taro 0）

---

## 六、待推广（阶段二 · 按优先级）

### 6.1 后台剩余 emoji + 硬编码（19 文件）

emoji 出现在（需逐个改 `NavIcon` 或去 emoji，颜色改 `var(--token)`）：
`MerchantLayout`(商家端侧栏)、`CommissionGuide`、`FinanceDashboard`、`Ledgers`、`Members`、
`Orders`、`Users`、`SymptomRules`、`MarketingTemplates`、`SelfStores`、`merchant/*`(Ads/Analytics/EmotionStudio/Index/Members/Messages/Orders/Products/Withdraw/Settings)。

> 注：`utils/emotion.ts`、`lib/supabase.ts` 的 emoji 属日志/文案，非 UI，可不改。

### 6.2 小程序残留内联色（抽语义类或改 `hsl(var(--token))`）

`payment`(语义色保留)、`user`(品牌渐变 #FFF0E8/#FFFBF7 可用 `.emotion-bg`)、
`index`、`product`、`store-home`、`merchant-*`、`explore`、`emotion-*` 等。
建议：新增 `--gold`(金豆琥珀)、`--danger-soft`(浅红底) 等语义 token，替代散落 `#`。

---

## 七、后续开发约定

1. 后台新页面：颜色**只用** `var(--token)`，图标**只用** `<NavIcon name="..." />`，**禁止新增 emoji 图标**。
2. 小程序新页面：用 Tailwind 类（`bg-background`/`text-foreground`）或 `hsl(var(--token))`，禁止裸 `#`。
3. 品牌名统一用「来电有喜」，后台副标题用「管理后台」。
4. 任何 UI 改动先对照本规范，确保三端调性一致。

---

## 八、验证记录

- `admin-web`: `npx tsc -b --force` → EXIT 0；`npx vite build` → EXIT 0
- 小程序：`taro build --type weapp` → EXIT 0
- 改动文件：`admin-web/src/index.css`、`admin-web/src/components/Layout.tsx`、`admin-web/src/components/icons.tsx`、`admin-web/src/pages/Dashboard.tsx`、`admin-web/src/pages/Login.tsx`、`src/pages/payment/index.tsx`

# 2026-07-16

## 食材食疗智能导购 · Phase 2 前端显形（完成）
- 用户问"是否达到智能化"→ 结论：当前是规则级智能，前端未接故用户无感知；追问"如何完美结合"→ 定调「LLM 做翻译润色、规则引擎是唯一决策方、合规闸门是最后闸门」融合架构（本期只上规则引擎，LLM 网关 ai-nlu/ai-copy 待后续）。用户选「先建 Phase 2 前端」。
- 新增 `src/contexts/FoodTherapyContext.tsx`：FoodTherapyProvider 挂 `src/app.tsx`（Auth/Location 内层）+ `useFoodTherapy()` → {selectedRuleId, setSelectedRuleId, activeRule, scoreProduct, rules}。选中态持久化 Taro storage('foodTherapyConstitution')，全站共享同一 active rule；未选时用中性默认 rule（不判慎点）。
- 首页 `src/pages/index/index.tsx`：公告栏后插入「🍵 我的状态/体质」筛选栏（4大类胶囊，来自 SYMPTOM_RULES）+ 选中后「🤖 智能推荐」三栏（优先推荐/少量慎点/不建议点，FitCard/RecSection 横向滚动小卡带食疗分徽标）。
- 详情页 `src/pages/product/index.tsx`：原料分析后插入「🤖 食材食疗导购」5 板块（①食疗适配分+性味+标签 ②情绪治愈分+标签 ③人群风险判定 ④智能搭配 match_goods+加料建议 ⑤合规免责）；share 标题选中状态时带「食疗适配X分·档位」。
- 购物车 `src/pages/cart/index.tsx`：goCheckoutAll 结算前调 checkCartConflicts，有冲突弹自研 modal（温补叠加/寒热对冲/同属性过量/相克慎搭，warn/danger 分级，去调整/仍要结算）。
- 体质档案页 `src/pages/constitution/index.tsx`（新）+ `app.config.ts` 注册 + 用户页「珍宝库」入口。
- 食疗海报 `src/utils/food-therapy-poster.ts`（新）：generateFoodTherapyPoster 用 Canvas 画 500×640 适配卡；详情页「生成食疗海报」按钮→存相册。
- 验证：`taro build --type weapp` 全量编译通过（无报错，constitution 等产物生成）；引擎逻辑自测 31/31 绿。
- 待用户本机：执行迁移 00100_food_therapy_fields.sql + 重部署 product-mutate 函数，商家填写 overall_nature/health_tag 等字段后智能度才完整。前端已能从 ingredients 兜底，迁移未执行也能跑（仅原料聚合性味+中性默认效果）。

## 热重载报错修复（useMemo is not defined）
- 微信开发者工具热重载抛 `ReferenceError: useMemo is not defined`（pages/index/index.js）：Phase 2 首页三栏用了 `useMemo`（L173 算 recommendGroups），但 React 导入漏了 `useMemo`（原 `import { useState, useCallback, useEffect, useRef }`）。
- 坑点：**Taro build 对此类"未导入的 React Hook"不报错，只在运行时崩**（Webpack 把它当普通标识符，编译期不校验）。
- 修复：`src/pages/index/index.tsx` 第2行补 `useMemo` → 重新 `taro build --type weapp` 通过（24.15s，零报错）。验证编译产物 `dist/pages/index/index.js` 中 `useMemo` 已从 `t.reactExports.useMemo` 正确解构（非裸标识符），根因消除。
- 教训：新增 React Hook 用完后，务必核对顶部 import 清单；此类问题构建期发现不了，要查编译产物符号引用或直接跑 devtools。

## 系统化闭环补全（知识录入端，完成）
- 用户问"是不是应该做成系统化"→ 确认闭环在「知识录入与配置」断链（商家无 UI 填导购字段、规则硬编码）。本次补齐最关键的商家后台录入。
- `admin-web/src/types/index.ts`：Product 接口新增 6 导购字段（与 00100 迁移一致）。
- `admin-web/src/utils/food-therapy-tags.ts`（新建）：本地镜像标签库 HEALTH_TAGS(9)/EMOTION_TAGS(8)/NATURE_SCALE(6)+parseList/joinList。镜像自小程序 `src/utils/food-therapy/types.ts`，改一侧需同步另一侧。
- `admin-web/src/pages/merchant/Products.tsx`：商品编辑弹窗「🥗 原料成分分析」后新增「🍵 食材食疗导购属性」区块（整体性味下拉/食疗标签多选/情绪标签多选/推荐搭配/相克提示/辅料加料）；保存写库带新字段 + **导购列软降级**（迁移00100未执行剥离重试）；表格行加「导购已配」绿标。该页直连 `supabase.from('products')` 写库（无独立 API 函数），与小程序 api.ts 同源策略。
- 验证：`cd admin-web && tsc -b && vite build` 全过（TSC_EXIT=0, VITE_EXIT=0，仅 chunk>500kB 提示性警告）。
- 闭环现状：商家录入✅→规则引擎✅→小程序展示✅→用户反馈回流⚠️(体质档案未回流)。剩余系统化扩展：症状规则库迁DB+运营配置页、营销模板后台、刷卡后台话术库、LLM网关(ai-nlu/ai-copy)。
- 交付说明：`deliverables/食材食疗导购_系统化闭环.md`。

## 症状规则库迁 DB 可配置（系统化的"知识配置端"下一步，完成）
- 用户"继续"→ 推进系统化，做最关键的「规则库可配置」（此前硬编码于 symptom-rules.ts，运营改规则需发版）。
- 迁移 `00101_symptom_rules.sql`：建 symptom_rules 表（id/category/label/keywords[]/priority_health_tags[]/ban_natures[]/ban_health_tags[]/remind_text/is_active/sort_order）+ 种子现有12条（与 symptom-rules.ts 一致，幂等 upsert）；**DISABLE RLS**（运营配置表，测试期同 symptom 性质）。须用户本机执行。
- 引擎 `src/utils/food-therapy/symptom-rules.ts`：默认集改名 `DEFAULT_SYMPTOM_RULES`；新增模块级 `ACTIVE_RULES` + `getActiveRules()/setActiveRules(rules)`；`resolveSymptomRule/getRuleById/listRulesByCategory` 内部分改用 `getActiveRules()`；保留 `SYMPTOM_RULES` 别名导出兼容既有 import。
- `src/contexts/FoodTherapyContext.tsx`：挂载时 `supabase.from('symptom_rules').select().eq('is_active',true)`，成功后 `setActiveRules(mapRowToFitRule)` + `rulesTick+1` 触发 activeRule/rules 重算；**表不存在/失败静默回退默认集**（向后兼容，迁移未执行小程序完全正常）。
- `admin-web`：food-therapy-tags.ts 加 `SymptomRule` 接口 + `SYMPTOM_CATEGORIES` + `categoryLabel`；新建 `pages/SymptomRules.tsx` 运营配置页（列表/新增/编辑/删除/启停，暗色风格与 Layout 一致，直连 supabase 读写）；`App.tsx` 注册路由 `/symptom-rules` + `Layout.tsx` NAV 加「🍵 食疗规则库」入口。
- 验证：admin-web `tsc -b && vite build` 全过（TSC=0, VITE=0）；`taro build --type weapp` 进行中；引擎 self-test 31/31 绿。
- 影响：运营在 admin-web 改规则 → 小程序端下次冷启动实时生效（无需发版）。闭环「知识配置」端从"改代码"升级为"后台可视化运营"。
- 待用户本机：执行迁移 `00101_symptom_rules.sql`（之后小程序才从DB读规则；未执行则继续用硬编码默认12条）。

## 迁移跑完后 live 实测（下午）
- 用户确认迁移已执行。我用 anon key 直连真库 REST 实测得两项结论：
  - `products` 新列(00100) ✅：SELECT 返回 overall_nature/health_tag/emotion_tag/match_goods/conflict_goods/aux_remind 列均存在（值空但列在），数据层通。
  - `symptom_rules`(00101) ⚠️ **实际与迁移意图不符**：表存在，但 RLS 处于 ENABLE 且无 policy → anon SELECT 返回 `[]`、anon INSERT 报 `42501 new row violates row-level security policy`。即 admin-web「食疗规则库」页打开会空白、保存被拒；小程序端因 DEFAULT_SYMPTOM_RULES 硬编码兜底仍能跑（但后台改规则不进 DB）。可见 00101 的 `ALTER TABLE ... DISABLE ROW LEVEL SECURITY` 本机执行时未真正生效（漏跑/回滚）。
- admin-web 代码侧全就绪：构建 `tsc -b && vite build` 通过(BUILD_EXIT=0)；`vite preview` HTTP 200 返回 index.html（能正常打开）；`.env` 配 VITE_SUPABASE_URL+anon、VITE_USE_MOCK=false；路由/导航/读写逻辑正确。
- **待用户本机修复（沙箱无 supabase CLI）**：Dashboard SQL Editor 跑 `ALTER TABLE public.symptom_rules DISABLE ROW LEVEL SECURITY;`（幂等）。之后我可经 REST API 直接补种12条默认规则并复查 SELECT=12行；或用户直接重跑 00101 全量（DISABLE RLS + 幂等 upsert 种子，可一次性自愈）。
- **✅ 已闭环（用户重跑 00101 全量后复查）**：anon 直连 SELECT `symptom_rules?select=id,label,category,is_active&order=sort_order` 返回 **12 条全量规则（均 is_active=true）**，RLS 已关、读写通。至此 products 新列(00100)✅ + symptom_rules 12 条(00101)✅ + admin-web 构建/打开✅ + 小程序端从 DB 加载规则✅。食材食疗智能导购系统化「知识配置端」完整可用，运营改规则→小程序冷启动实时生效。

## 系统化 100% 全覆盖（Module A-D，代码完成待验证）
- 用户"全覆盖"指令 → 把 4 块剩余增强全部补齐。
- **Module A 营销模板后台可编辑**：`supabase/migrations/00102_food_therapy_templates.sql`（5 条种子 sales_word/detail_desc/circle_copy/risk_tip/poster_template，含 `{name}{natureText}{tagText}{tagSentence}{remindText}` 占位符，DISABLE RLS）；`src/utils/food-therapy/templates.ts` 新增 `DEFAULT_TEMPLATES`/`ACTIVE_TEMPLATES` 变量 + `getActiveTemplates/setActiveTemplates/getTemplateContent/buildFillVars/fillTemplate` + `NATURE_DESC`；`marketing.ts` 改用模板机制，缺模板回退硬编码；`FoodTherapyContext` 挂载时从 DB 加载模板；admin-web `MarketingTemplates.tsx` 配置页 + 路由 + 导航。
- **Module B 用户反馈回流/千人千面**：`supabase/migrations/00103_food_therapy_feedback.sql`（user_id/product_id/event_type CHECK(view/add_cart/purchase/like/dislike)/health_tag[]/emotion_tag[] + 3 索引，DISABLE RLS）；`api.ts` 加 `trackFoodTherapyEvent`(best-effort insert, catch 静默) + `getUserFoodTherapyWeights`(聚合 health_tag 正负权重：view 不计、like/add_cart/purchase +1、dislike -1)；`scoring.ts` `scoreFoodTherapy(input,rule,weights?)` 匹配标签加分 `15+clamp(w,-10,20)` 并展示偏好明细；`FoodTherapyContext` 加载 `weights` 并透传 `scoreProduct`。
- **Module C 收银台话术库+结算风险看板**：`product/index.tsx` 埋 view/like/dislike/add_cart 事件 + 顶部新增「🗣 导购话术」卡(`mkt.short_sales_word` + ✨AI 润色按钮→`generateMarketingCopyLLM` 写 `aiSalesWord`)；`payment/index.tsx` 新增 `riskModal` + `computeCheckoutRisks`(购物车冲突 `checkCartConflicts` + avoid 档体质禁忌) 在 `handlePay` 前拦截，二次确认置 `_riskAck` 跳过，下单成功埋 purchase。
- **Module D LLM 网关(ai-nlu/ai-copy)**：`src/utils/food-therapy/llm.ts` `nluParseSymptoms`(调 food-therapy-ai mode=nlu，失败回退 `resolveSymptomRule` 关键词) + `generateMarketingCopyLLM`(mode=copy，失败回退 `generateMarketingCopy`)；`constitution/index.tsx` 顶部「说说你现在的感受」自由文本→NLU→`setSelectedRuleId`；`supabase/functions/food-therapy-ai/index.ts` Deno Edge Function（mode=nlu/copy + `hasMedicalClaim` 合规闸门回退 + 复用 `LLM_API_KEY/LLM_BASE_URL/LLM_MODEL`）。
- **千人千面算法闭环**：体质(selectedRuleId 驱动 symptom_rules 的 ban_natures/ban_health_tags 减分 + NLU 自由文本映射) → 口味(health_tag/emotion_tag 匹配 +15 基础分 + 反馈权重加权) → 千人千面(per-user `food_therapy_feedback` 聚合 → `getUserFoodTherapyWeights` → 同 SKU 不同用户打分不同，无训练模型轻量统计方案)。
- **状态**：4 块代码已写完，但 `pnpm install` 被用户中断、**尚未跑构建验证**，存在潜在 TS 错误待修复。用户本机仍须：执行 00102+00103 迁移 + `supabase functions deploy food-therapy-ai`。

## 构建验证收口（16:2x）
- 修复两处：① `admin-web/src/pages/MarketingTemplates.tsx` 删未用 `ReactNode` import（`tsc -b` 原报 TS6133）；② `src/utils/food-therapy/llm.ts` 顶层 `import { supabase } from '@/client/supabase'` 改函数内**惰性动态 import**，根除"裸跑 node 自测时加载 @tarojs/runtime 触发 `ENABLE_INNER_HTML is not defined`"崩溃（constitution/product 页仍从 barrel 正常导入 nluParseSymptoms/generateMarketingCopyLLM）。
- 验证结果：小程序 `taro build --type weapp` ✅ BUILD_EXIT=0（24.43s，仅 supabase.ts dynamic import 警告）；admin-web `tsc -b` ✅ TSC_EXIT=0 + `vite build` ✅；引擎自测 `food-therapy-self-test.ts` **31/31 绿**。Module A-D 代码全部编译通过。
- **部署问答**：用户问 `supabase functions deploy food-therapy-ai` 怎么部署。确认函数用 `jsr:@supabase/supabase-js@2` + `Deno.serve`，无需 deno.json，与仓库其他函数同构；客户端 anon key 调 `functions.invoke('food-therapy-ai')` 默认鉴权可过。部署须用户本机（沙箱无 CLI）。

## 部署卡点（16:3x，重要）
- 用户要求"自己完成"部署 → 我尝试在沙箱自装 supabase CLI。**根因确认**：`npm i -g supabase` 失败与网络无关，是 wrapper 装好后去 GitHub 拉 win32 二进制被沙箱掐带宽。
- **沙箱网络实测**：到 GitHub releases 仅 ~34KB/s（75MB 包 120s 仅下 4MB），到 npm registry CDN 实测 24 B/s（几乎断流）。单次命令上限 10 分钟 → 75MB 包（≈38min）**不可能在沙箱内拉完**。
- **结论**：supabase CLI 二进制 + 函数部署 + 迁移执行**只能用户本机做**（其浏览器/网络快）。沙箱内无法自完成部署。
- 已给用户交付 `deliverables/food-therapy-ai-deploy.md`（Windows 部署手册）。本机最快路径：浏览器下 `supabase_2.109.1_windows_amd64.zip` → 解压 `supabase.exe` 到 `C:\tools\supabase\` → `supabase login`（浏览器授权一次）→ `supabase link --project-ref pyqgsxcjmijtbstwthbn` → `supabase functions deploy food-therapy-ai`；00102/00103 在 Dashboard SQL Editor 粘贴执行（DISABLE RLS 需保）。
- project-ref = `pyqgsxcjmijtbstwthbn`（来自 .env 的 SUPABASE_URL）。

## 去 AI 字样（18:5x，纯文案替换，功能不动）
- 用户"不能带AI"→ 澄清为"把 AI 标识的字去掉换个词"（非删功能）。扫描前端可见文案：仅 `src/pages/product/index.tsx:284` 有可见 `· AI编译`（其余 `AI 卖点标题` 均为代码注释，不可见）。
- 替换：`product/index.tsx` `· AI编译` → `· 智能生成`；5 处注释 `AI 卖点标题` → `智能卖点标题`（cart / emotion-detail .tsx+.scss / merchant-emotion-compile / product）。LLM 底层（emotion-compile / food-therapy-ai / llm.ts）全部保留。
- 验证：`taro build --type weapp` ✅ BUILD_EXIT=0（23.22s）。前端已无可见 "AI" 字样（grep `AI编译|AI 卖点标题|· AI` 零命中）。

## 首页"体质/身体状态"筛选器设计评审（19:0x，用户问意见，未改）
- 现状（`src/pages/index/index.tsx:469-524`）：顶部一张卡 `🍵 食疗智能推荐` + hint `勾选你当前的身体状态，自动匹配专属食疗推荐` + 「你的身体状态（可多选）」7 chip（宫寒量少/经期量大/喉咙肿痛/易上火/体虚怕冷/痛风/脾胃虚寒）+「当前场景（单选）」6 chip（熬夜工作/秋冬降温/经期调理/术后恢复/单人简餐/饭后解腻）+ 选中后三栏 `🤖 智能推荐`。
- 我的结论：功能合理（人群+场景驱动三栏 classifier），但呈现过重、偏"医疗问卷"，且与"无 AI"诉求有**一处直接矛盾**：`🤖` emoji 是最强 AI 暗示符号，应换 ✨/🍵/🥗。
- 两个真问题：① 首屏空值——`tierResult` 在 `selectedCrowds.length===0` 为 null，新用户进首页下方三栏整体不渲染，首屏只见大筛选卡；② 认知负担——让用户自判"宫寒/易上火"需中医自诊能力，高摩擦。
- 建议（优先级）：1) 去 🤖；2) 首屏必有内容（未选时默认"热门/为你精选"，选后才切三栏）；3) 弱化医疗调性（标题改"今日为你而选"、场景优先默认只露、身体状态收折叠"更多"）；4) chip 压成横滑或底部弹层。已提议先改 1+2。

## 首页筛选器落地改造（19:1x，已改，待构建验证）
- 按用户"继续"批准，落地 ①+②：
- ① 去 AI 调性：筛选卡标题 `🍵 食疗智能推荐` → `🍵 食疗推荐`；三栏区标题 `🤖 智能推荐` → `🥗 为你适配`（选中态）/ `🥗 为你精选`（默认态）。全站再无 `🤖` 符号。
- ② 首屏必有内容：原 `{tierResult && (...)}` 整体隐藏改为三元分支——`tierResult` 非空显示三栏适配；为空（未选人群）显示 `🥗 为你精选` 横向商品流（`feedItems.slice(0,12)`，复用 FitCard，点"选状态 ›" `Taro.pageScrollTo` 回顶到筛选器）。筛选是"锦上添花"而非"前置门槛"。
- 注：`classifyProduct` 语义未动（人群为空即返回 null，场景仅作二级 refine，符合 spec 模型）；未做"身体状态收折叠"等 ③ 范围（用户仅批 ①+②）。

## get_rank_progress 返回类型冲突修复（SQL 42P13）
- 用户在本机 Supabase SQL Editor 重跑 get_rank_progress 时报 `42P13 cannot change return type`：库里已存在 `get_rank_progress(uuid) RETURNS TABLE`，而他要建的 jsonb 版本冲突。
- 根因：历史迁移签名不一致——00005=`p_user_id`+jsonb（前端 rpc 实际调用），00013/00049/00050=`user_id`+`RETURNS TABLE`（与前端参数名/返回类型都不符）。前端 `src/pages/my-promotion/index.tsx:70` 用 `rpc('get_rank_progress',{p_user_id})` 且消费 jsonb 字段（current_rank/next_rank/direct_count/target_count/progress/total_gmv/points/balance）。
- 修复：新建 `00106_consolidate_get_rank_progress.sql`（先 DROP 所有重载再重建唯一 jsonb(p_user_id) 版本，段位按 V5 消费阈值 0/200/800/2000/6000/20000，进度=距下一段位消费%),并把 00013/00049/00050 的函数体全部对齐成同一份 jsonb(p_user_id) 版本（00005 本就一致）。
- 用户本机待执行：`00106`（修复 live DB）。重跑迁移链时五个迁移最终都收敛到 jsonb(p_user_id)，不再冲突。
- 段位算法说明：RPC 版用纯消费额阈值（V5），前端另有同算法的降级分支；长老/掌门徽章软门槛仅前端降级分支实现、RPC 版未纳入（可接受，RPC 为增强、降级为权威展示）。
- 构建验证：`taro build --type weapp` ✅ BUILD_EXIT=0（24.13s）。首页改造完成。

## 体质档案个性化（用户新需求，19:2x，代码完成待双构建验证）
- 用户诉求：体质应是"用户自己填体质/健康信息 → 配对商品；按每次消费记录总结推荐。如高血压推荐商品"。选范围=先建档案+自动匹配；医学标签=食养参考+免责声明（禁治疗/降血压措辞）。
- 数据模型：`profiles.constitution_tags text[]`（迁移 `supabase/migrations/00105_profile_constitution.sql`，GIN 索引；**需用户本机执行**，沙箱限带宽）。`Profile` 类型加 `constitution_tags`；`updateProfile` 扩 `Pick<...,'nickname'|'avatar_url'|'constitution_tags'>`。
- 词表扩展：`food-therapy/types.ts` + admin-web `food-therapy-tags.ts` 均拆分 `BODY_CROWD_OPTIONS`(7) + `HEALTH_CROWD_OPTIONS`(高血压/高血糖/高血脂/肠胃虚弱/失眠/免疫力低)，合并为 `CROWD_OPTIONS`。使商家可在后台 rec/cautious/forbidden_crowds 标高血压等，前端可勾选匹配。
- 自动匹配：`FoodTherapyContext` 内 `useAuth()` 读 `profile.constitution_tags`，登录后一次性注入 `selectedCrowds`（seededRef 防重复），首页/详情食疗三栏常驻，无需每次手选；手动调整仍存 storage。
- 档案录入页：`constitution/index.tsx` 重写为「我的体质档案」——身体状态 + 健康状况两组 chips，保存时 `updateProfile({constitution_tags})` 持久化 + toast；加「食养参考·不替代医嘱」免责声明。场景(ephemeral)移出该页，仅留首页。
- 首页筛选卡加小字免责「食养参考 · 不替代医嘱」。
- `taro build --type weapp` ✅（24.53s）+ `admin-web tsc -b && vite build` ✅（6.46s）双构建均通过，体质档案个性化整批代码编译验证完成。
- 消费总结推荐（按订单聚合→推导偏好食养方向）属后续范围，本次未做（用户仅批"先建档案+自动匹配"）。
- 合规红线再确认：高血压是医学病名，仅作人群匹配维度 + 免责声明，绝不出现"降血压/治疗/治愈"等词。
- 验证：`taro build --type weapp` ✅ BUILD_EXIT=0（24.13s，dist/pages/index/index.js 27.77kB）。首页改造完成。

## 首页合并「情绪+体质」输入 + 移除独立体质页（20:1x）
- 用户新指令："首页全部结合，输入情绪、体质状况，而推荐商品和食品。移除我的体质模块。"
- 实现：① 删除独立体质页 `src/pages/constitution/index.tsx` + `app.config.ts` 路由 + `user/index.tsx`「我的体质档案」菜单项（模块移除）。② 首页 `pages/index/index.tsx` 把「情绪输入区」卡与「食疗推荐筛选器」卡**合并为一张「今天的状态」卡**：含心情输入+快捷词（情绪）、身体状态多选 chips（体质，CROWD_OPTIONS）、当前场景单选 chips（SCENE_OPTIONS）；头部"清空"改为同时清空情绪+筛选。③ 下方保留两个推荐维度：体质分档三栏（🥗为你适配，按 selectedCrowds 分 recommend/caution/avoid）+ 情绪 Feed（情绪专属推荐/为你精选）。
- 保留项：profile.constitution_tags 字段 + FoodTherapyContext auto-seed（登录后把 profile 标签预填首页 chips，作为"记住我的身体状态"便利，非独立页）；updateProfile 仍被 user 页昵称使用；symptom-rules.ts 仍被 llm.ts 引用，未动。
- 合规：合并卡底部保留「食养参考 · 不替代医嘱」小字。
- 验证：`taro build --type weapp` ✅ BUILD_EXIT=0（25.24s，dist/pages/index/index.js 27.59kB）。首页合并+移页完成。

## 消费记录总结推荐（20:2x，落地，taro build 通过）
- 用户"继续" → 推进上轮留的「消费记录总结推荐」（其最早设想：按历史订单聚合偏好再推荐，如高血压→推荐食物）。
- 设计取舍（合规优先）：**只聚合中性维度，不做疾病/体质推断**。从已购订单回溯商品 → 聚合「食养功效标签 health_tag 频次(取前3) + 整体性味 overall_nature 众数」。高血压等医学病名**绝不**作为推断结论出现，仅作为商品既有 health_tag 的偏好信号，间接达成"同方向推荐"。区块附「食养参考·不替代医嘱」。
- 新增 `src/utils/consumption-profile.ts`：`analyzeConsumption(products)` 聚合画像 + `recommendByConsumption(pool, profile, boughtIds, limit)` 打分推荐（health_tag 命中按排名加权、nature 命中+1，排除已购，无信号返回 []）。纯函数、零网络、可解释。
- `src/db/api.ts` 新增 `getProductsByIds(ids)`：复用 products+stores 联合查询，`.in('id', uniq)` 批量取，供回溯已购商品。
- 首页 `src/pages/index/index.tsx`：登录后 `loadConsumptionProfile()`（getOrders 取 ≤50 单→回溯 product_id→getProductsByIds→analyzeConsumption）；新增 `🛒 根据你的常买好物` 区块（展示偏好食养标签 chips + FitCard 横滑 + 免责），复用 feedItems 作候选池（无额外网络请求）。未登录/无订单/无信号则不渲染。
- 验证：`taro build --type weapp` ✅ BUILD_EXIT=0（24.45s，仅 supabase.ts dynamic import 警告；dist/pages/index/index.js 30.54kB，+2.9kB 为本功能）。
- 与 Module B(00103 food_therapy_feedback 主动反馈加权) 区别：本次是**被动消费史聚合推荐**，轻量、无新表、无需部署；Module B 仍需本机执行 00103 迁移后启用。两者可并存。

## 开发收口（用户确认"全部完成"，20:2x）
- 食材食疗智能导购整条开发线全部代码完成 + 双构建（taro weapp / admin-web tsc+vite）全绿。开发侧正式收口。
- 余下**仅本机部署动作**（沙箱限带宽，无法代跑），清单：① `00102`/`00103`/`00105`/`00106` 在 Supabase SQL Editor 执行；② `symptom_rules` 确认 DISABLE RLS（之前 00101 漏跑已补种12条）；③ `supabase functions deploy food-therapy-ai`（LLM 网关，可选）；④ 商家后台填商品导购字段（health_tag/overall_nature 等）后智能度才完整。
- 功能启用依赖：消费推荐需「有带 health_tag 的已购订单」；体质 auto-seed 需 00105 + profile 已填标签；千人千面需 00103。未部署时各功能均优雅降级（不崩、不空屏）。

## 迁移文件梳理与冗余清理（20:2x，已执行）
- 用户"梳理数据库、删冗余多余"→ 扫描 `supabase/migrations/`（原 99 个文件）。
- 删除 5 个冗余/非迁移文件：`00013_update_get_rank_progress.sql`（整文件仅 get_rank_progress 重定义，与 00106 重复）、`00050_lower_rank_thresholds.sql`（同上）、`DIAG_verify_products_rls.sql`/`DIAG_verify_orders_rls.sql`/`VERIFY_00095_rls.sql`（纯 SELECT 诊断脚本，非迁移）。现存 94 个。
- 手术清理 `00049_remove_team_performance.sql`：摘除其中完整重定义的 `get_rank_progress` 函数块，仅保留 `ALTER TABLE profiles DROP COLUMN IF EXISTS team_performance;`（列删除是必要变更）。
- 结果：`get_rank_progress` 现在只由 `00005`（首次创建 jsonb p_user_id）+ `00106`（末位 DO$$ drop-all + create，权威）两处定义，中间无重复；42P13 冲突根因消除，且早期间局部部署仍安全（函数由 00005 创建、00106 收口）。
- 仅编号撞车但内容各自独立（不删，仅提示）：`00019`×2（加 orders.store_id vs 回填 mood_tags）、`00035`×2（加 commission_distributed 字段 vs 建 pending_referrals 表）、`00049`×2（videos 桶 vs 删 team_performance）、`00050`×2（product_emotion 字段 vs 降段位阈值）。属两套迁移流撞序号，内容不冲突。
- 提示过的潜在更深清理（未做，可选）：`00021_merge_patch`/`00022_fix_products_fields`/`00023_full_patch`/`00024_fix_image_storage`/`00025_set_test_image`/`00029_*`/`00030_*`/`00031_*`/`00033_check_all_tables` 这类"补丁链"可能存在被后续覆盖的冗余，需逐个读才能确认，风险较高，留待用户决定。
- 删除安全性：config.toml 存在（supabase 追踪启用），但用户一直 SQL Editor 手动粘贴执行、未走 `supabase migration up`，故删本地文件不影响线上库；函数删除后由 00106 收口重建。

## 迁移重排 + 线上库待跑盘点（20:3x）
- 用户"重新排列撞序号文件，然后看哪个需要重新SQL"。
- 重排（依赖安全优先）：`00035_add_pending_referrals` 不能挪——`00081_production_rls_hardening`/`00095_consolidated_rls_final` 遍历表名含 `pending_referrals`，须在其前建表；同序号 `00035_add_commission_distributed_to_orders` 末尾有 `ALTER orders DISABLE RLS` 也不能放末尾（会覆盖 00095 强化）。故改挪后者到空闲槽 00063（仍在 00081 前）。最终重排：
  - `00019_auto_tag_products.sql` → `00107_auto_tag_products.sql`（mood_tags 回填，靠后安全）
  - `00035_add_commission_distributed_to_orders.sql` → `00063_add_commission_distributed_to_orders.sql`
  - `00049_create_videos_bucket.sql` → `00109_create_videos_bucket.sql`（存储桶，无依赖）
  - 留原号：`00019_add_orders_store_id` / `00035_add_pending_referrals` / `00049_remove_team_performance`
- 重排后 `000XX` 前缀全唯一（仅 `20260705_` 两个日期迁移共享前缀、互不冲突且排末尾，无需动）。现存 94 个迁移。
- **线上库真实探测（anon key REST，沙箱可通 supabase.co）结论**：
  - `00104` products.rec_crowds → 42703 不存在 ❌ 未跑
  - `00105` profiles.constitution_tags → 42703 不存在 ❌ 未跑
  - `00102` food_therapy_templates 表 → 200 存在 ✅ 已跑
  - `00103` food_therapy_feedback 表 → 200 存在 ✅ 已跑
  - `00106` get_rank_progress → 函数存在但执行报 `referrer_id does not exist`（profiles 缺 referrer_id 列，函数体挂掉）⚠️
- **关键缺口**：`profiles.referrer_id` 列缺失！00106 的函数体与首页/推荐无关，但 my-promotion 段位页依赖它；且 `00019_add_orders_store_id` 里 `ALTER orders ADD COLUMN referrer_id` 是 orders 表不是 profiles 表。需查明谁该给 profiles 加 referrer_id（可能漏了某迁移或需在 00106 前补 `ALTER profiles ADD COLUMN referrer_id`）。这是下一轮待办。
- 待用户本机执行清单（更新）：**必须** 00104、00105、00106（且 00106 前须确保 profiles.referrer_id 存在）；00102/00103 已确认线上存在，无需重跑。

## 本机执行包生成（20:4x）
- 用户"需要"→ 生成可直接贴 Supabase SQL Editor 的一次性执行包 `supabase/deploy-bundle-2026-07-16.sql`。
- 内容四段（均幂等 IF NOT EXISTS / DROP...CASCADE 可重复跑）：
  - **Part 0**：补 `profiles.referrer_id`（定义严格对齐 00005: `uuid REFERENCES auth.users(id) ON DELETE SET NULL`）。根因：线上 profiles 表仅进了 referral_code（00005 同句 ALTER 加两列却漏了 referrer_id），致 00106 函数体 `WHERE referrer_id=...` 执行报 does not exist。
  - **Part 1**：00104 products 食疗字段（food_category/positive_effect/risk_warning/emotion_copy/scenes/rec_crowds/cautious_crowds(+notes)/forbidden_crowds(+reasons)/combo_product_ids/guide_sentence/moments_copy/taboo_warning + 5 GIN/btree 索引）。
  - **Part 2**：00105 profiles.constitution_tags + GIN 索引。
  - **Part 3**：00106 get_rank_progress jsonb(p_user_id) 收敛（DO$$ drop-all + create，段位 V5 阈值）。
- 附注：00102/00103 线上已存在（200）无需重跑；文件末尾附"执行后自检"4 条 SELECT/RPC 验证语句。
- 文件位置：`C:\Users\zhanglin\Desktop\app-coobohaoham9\supabase\deploy-bundle-2026-07-16.sql`。

## 执行包实测验证（20:5x，部分成功）
- 用户"全部跑完了"→ 我 anon REST 复探线上四项：
  - ✅ `products.rec_crowds`(00104) → 200，Part1 生效
  - ✅ `profiles.constitution_tags`(00105) → 200，Part2 生效
  - ❌ `profiles.referrer_id`(Part0) → 仍 42703 不存在，**Part0 未生效**
  - ❌ `get_rank_progress()`(Part3) → 仍报 `column "referrer_id" does not exist`，函数虽建但底层列缺
- 诊断：Part0 那句 `ALTER ... ADD COLUMN referrer_id uuid REFERENCES auth.users(id) ON DELETE SET NULL` 极可能在 SQL Editor 执行时（外键/auth schema 交互）报错被跳过，或该段未粘贴进。Part1/2 不依赖此列故成功；Part3 函数 CREATE 成功（plpgsql 懒解析不校验内部列），仅调用时崩。
- 处置：另出 `supabase/fix-referrer-id.sql`（带外键主版 + 无外键 `uuid` fallback），请用户**单独执行**该句并确认无红色报错，之后我复探闭环。

## 首页「身体状态」并入心情输入区（20:5x，已改，taro build 通过）
- 用户"你的身体状况应该与输入心情一样，合并在一起"→ 把首页「今天的状态」卡里"身体状态"从分隔线下方独立模块，并入"心情输入"区，合成一个统一输入。
- `src/pages/index/index.tsx` 改动：① 删除原"身体状态"前的分隔线 `<View h-px>` + 粗体重复标题「你的身体状态（可多选）」；② 将身体状态多选 chips（CROWD_OPTIONS）**上移到心情输入框正下方**（紧跟 input，位于情绪分析/诗意结果之前），标签改为轻量提示「身体状态（可多选，与心情一同影响推荐）」；③ 场景（单选）仍独立保留（概念不同）。"清空"按钮不变（同时清情绪+筛选）。
- 结构现为：快捷心情词 → 心情输入框 → 身体状态多选 → 情绪分析结果 → 诗意 → 场景单选 → 免责。身体状态与心情读起来是一个连续输入区，不再分家。
- 验证：`taro build --type weapp` ✅ BUILD_EXIT=0（35.18s，仅 supabase.ts dynamic import 无害提示；dist/pages/index/index.js 30.55kB，与合并前 30.54kB 持平，仅重排无新增逻辑）。

# 2026-07-06 工作日志

## 全面修复"加载中"永久显示问题 (commit 25c2b5e)

### 根本原因分析
"加载中"问题有多层原因，形成加载链：
1. `AuthContext.loading=true` 阻塞 `RouteGuard` 渲染
2. `RouteGuard` 自身也有 loading 判断
3. 各页面自己的 `loading` 状态可能永远为 true

### 修复内容

#### AuthContext.tsx
- `getSession()` 改用 `Promise.race()` 强制定时8秒
- 原来只有 `setTimeout` 清 timeout，现在用 `Promise.race` 从 Promise 层面杜绝挂起
- 修复 cleanup 闭包泄漏（cancelled 变量）

#### RouteGuard.tsx
- 超时从 5秒缩短为 3秒
- 加载超过 2秒显示已等待秒数（方便定位卡点）
- 3秒后强制渲染页面

#### user/index.tsx（侠客页）
- 新增 `profileLoading` 状态，区分"正在加载"和"加载失败"
- `merchantStatusNode` 改用 `profileLoading` 判断
- 修复 `loadData` 中 `!user` 时直接 return 但 `profileLoading` 未设为 false 的问题

### 测试结果
- ✅ 构建成功（16.11秒）
- ⚠️ 需要在微信开发者工具中重新编译测试

---

## 修复商家入驻页面(merchant-apply) loading 永久挂起 (commit 3db966b)

### 问题描述
商家入驻页面显示空白 + loading spinner，永远不消失。

### 根本原因
`merchant-apply/index.tsx` 的 `loadApp()` 函数调用 `getMyMerchantApplication()` 没有超时保护。当 Supabase API 挂起时，`setLoading(false)` 永远不会执行。

### 修复内容
1. 导入 `withTimeout` 工具函数
2. 给 `loadApp` 的 API 调用添加 5 秒超时保护
3. 超时时设置 `existing=null`（显示申请表单）而非永远 loading

### 测试结果
- ✅ 构建成功（16.09秒）
- ✅ 代码已推送到 GitHub (commit 3db966b)

---

## 修复商家入驻提交后状态不更新 (commit dfd7498)

### 问题描述
用户提交商家入驻申请（张林水果店）后，页面仍然显示申请表单，而不是显示"审核中"状态。

### 根本原因
1. `handleSubmit` 提交成功后直接 `navigateBack`，没有更新本地 `existing` 状态
2. 页面没有使用 `useDidShow` 在重新显示时重新加载数据
3. 用户从其他页面返回时，`loadApp()` 不一定会重新执行

### 修复内容
1. 添加 `useDidShow` hook，页面每次显示时重新加载申请状态
2. 修改 `handleSubmit`：提交成功后调用 `loadApp()` 刷新状态，显示"审核中"页面
3. 移除自动 `navigateBack`，让用户手动返回（看到审核状态后再返回）
4. 添加 try/catch 错误处理

### 测试结果
- ✅ 构建成功（16.20秒）
- ✅ 代码已推送到 GitHub (commit dfd7498)

---

## 全面修复所有管理后台页面 loading 挂起 (commit 5c818e5)

### 问题描述
用户反馈"一直加载中"，怀疑与管理后台有关。

### 修复范围
创建通用超时工具 `src/utils/withTimeout.ts`，批量修复：
- ✅ admin/index.tsx（武林盟首页）
- ✅ admin-merchants/index.tsx（门派大典）
- ✅ admin-products/index.tsx（宝贝审阅）
- ✅ admin-withdrawals/index.tsx（银票兑付）
- ✅ admin-ugc/index.tsx（武林贴管理）

---

## 修复 ReferenceError: View is not defined (commit 1ef7791)

### 问题描述
用户测试时发现小程序崩溃，控制台报错：`ReferenceError: View is not defined`

### 根本原因
`src/components/RouteGuard.tsx` 组件中使用了 `<View>` 和 `<Text>` 组件（用于显示加载状态），但文件顶部的 import 语句中缺少从 `@tarojs/components` 导入这些组件。

### 修复内容
在 `RouteGuard.tsx` 第4行添加：
```typescript
import {View, Text} from '@tarojs/components'
```

### 测试结果
- ✅ 构建成功（15.59秒）
- ✅ 代码已推送到 GitHub (commit 1ef7791)
- ⚠️ 需要用户在微信开发者工具中重新编译测试

---

## 修复 TypeError: Cannot read property 'merchant_status' of null (commit e57d964)

### 问题描述
用户测试时发现侠客页面崩溃，控制台报错：`TypeError: Cannot read property 'merchant_status' of null`

### 根本原因
`src/pages/user/index.tsx` 第114行：
```typescript
const status = profile.merchant_status || application?.status || 'none'
```
虽然前面有 `profileLoading` 判断，但当 API 返回 `profile=null`（比如网络错误或数据不存在）时，`profileLoading=false` 但 `profile=null`，访问 `profile.merchant_status` 会报 TypeError。

### 修复内容
将第114行改为使用可选链操作符：
```typescript
const status = profile?.merchant_status || application?.status || 'none'
```

### 测试结果
- ✅ 构建成功（15.30秒）
- ✅ 代码已推送到 GitHub (commit e57d964)
- ⚠️ 需要用户在微信开发者工具中重新编译测试

---

## 修复侠客页面商家入口永远显示"加载中..." (commit 49e24f2)

### 问题描述
侠客页面可以正常渲染，但商家申请入口区域一直显示"加载中..."，不会消失。

### 根本原因
`loadData` 函数中的 `Promise.all([getMyProfile(), getMyMerchantApplication(), getOrderCounts()])` 没有超时保护。当 Supabase API 请求挂起（网络慢、服务器响应慢）时，`setProfileLoading(false)` 永远不会执行，导致"加载中..."一直显示。

### 修复内容
给 `loadData` 添加 `Promise.race()` 超时保护（5秒）：
```typescript
const [p, app, counts] = await Promise.race([
  Promise.all([...]),
  new Promise((_, reject) => setTimeout(() => reject(new Error('loadData timeout')), 5000))
])
```

### 测试结果
- ✅ 构建成功（15.44秒）
- ✅ 代码已推送到 GitHub (commit 49e24f2)
- ⚠️ 需要用户在微信开发者工具中重新编译测试

---

## 修复 submitMerchantApplication 缺少 user_id (commit a5797af)

### 问题描述
用户提交商家入驻申请后，页面仍然显示申请表单而不是"审核中"状态。

### 根本原因
`submitMerchantApplication()` 插入 `merchant_applications` 时**没有传入 `user_id**，导致 `getMyMerchantApplication()` 用 `.eq('user_id', user.id)` 查不到记录。

### 修复内容
添加当前用户 ID 到插入数据中。

### 测试结果
- ✅ 构建成功（16.35秒）
- ✅ 代码已推送到 GitHub (commit a5797af)

---

## 🎉 修复商家入驻完整流程 - 数据库层面问题排查与修复

### 问题现象
1. 提交申请显示"申请已提交！"但数据库 `merchant_applications` 表为空（0条记录）
2. 控制台报外键错误：`violates foreign key constraint "merchant_applications_user_id_fkey"`
3. 后续报 RLS 错误：`new row violates row-level security policy`

### 根本原因（三层问题）

#### 问题1：merchant_applications 表结构不完整
- 表只有 `id` 和 `created_at` 两列，缺少 user_id/store_name 等关键列和外键

#### 问题2：RLS 未真正关闭
- Dashboard 中点了 Disable 但没保存，API 插入被 RLS 拒绝

### 解决方案
1. SQL Editor 中 DROP + CREATE 完整表结构（含外键）
2. `ALTER TABLE merchant_applications DISABLE ROW LEVEL SECURITY`

### 最终结果 ✅
商家入驻**完整流程跑通**：填写→提交→写入DB→显示"审核中"
- 测试用户：18701410500 / 张林水果店 / 餐饮
- submitMerchantApplication 添加了 insert 错误检查和日志 (commit d070020)

---

## ✅ 情绪系统5个层次全部完成

### 完成功能
1. **层次1：视觉层** - 商品详情页情绪标签展示（带颜色、图标）
2. **层次2：文案层** - 商家中心商品表单添加"智能生成描述"按钮
3. **层次3：推荐层** - 探索页添加情绪化推荐（基于用户偏好）
4. **层次4：营销层** - 营销活动创建添加情绪标签选择
5. **层次5：社交层** - 商品评价添加情绪标签选择

### 隔离功能完成
- 探索页：只显示平台自营门店（`platformFilter: 'only'`）
- 犒赏铺：只显示商家店铺（`platformFilter: 'exclude'`）
- 产品隔离：两个页面的产品不会混在一起

### 修改文件
- `src/utils/mood-tags.ts` - 情绪词库（6大分类、30+情绪词、10+场景标签）
- `src/utils/emotion-description.ts` - 情绪化描述生成器
- `src/utils/emotion-recommendation.ts` - 情绪化推荐引擎
- `src/pages/merchant-products/index.tsx` - 商品表单添加情绪标签+智能生成
- `src/pages/explore/index.tsx` - 探索页添加情绪化推荐
- `src/pages/merchant-campaigns/create/index.tsx` - 营销活动添加情绪标签
- `src/pages/review/index.tsx` - 商品评价添加情绪标签
- `src/pages/product/index.tsx` - 商品详情页展示情绪标签
- `src/db/api.ts` - submitReviews 添加 mood_tags 支持

### SQL脚本
- `sql/add_mood_tags_to_campaigns.sql` - 添加 mood_tags 字段到 marketing_campaigns 和 product_reviews 表
- `create_user_emotion_preferences.sql` - 创建用户情绪偏好表

### 测试结果
- ✅ 构建成功（16.72s）
- ⚠️ 需要用户运行SQL脚本并测试小程序

---

## 修复 marketing_campaigns 400 错误

### 问题现象
控制台报 `GET .../rest/v1/marketing_campaigns?store_id=eq.70778d6b... 400`

### 根本原因
前端错误查询 `store_id`，但表中只有 `city_id`（营销活动按城市发放）

### 修复内容
修改 `src/pages/merchant-campaigns/index.tsx`：
- 移除 `store_id` 查询
- 改为基于 `city_id` 查询（商家只能看到自己所在城市的活动）

### 测试结果
- ✅ 构建成功
- ⚠️ 需要用户测试营销活动页面是否还报错

---

## 添加商品视频上传功能

### 完成功能
1. **uploadVideo() 函数** - 添加到 `src/utils/upload.ts`
2. **视频上传 UI** - 添加到商品表单（详情图下方，商品描述上方）

### 修改文件
- `src/utils/upload.ts` - 添加 uploadVideo 函数
- `src/pages/merchant-products/index.tsx` - 添加视频上传 UI

### 需要手动操作
- 在 Supabase Storage 中创建 `videos` 存储桶（Public bucket）

---

## 项目当前状态

### ✅ 已完成的功能
- 商家入驻完整流程（提交→写入DB→显示"审核中"）
- 管理后台 admin 账号创建并可以登录
- 网页端商品保存限制已去掉
- LazyImage 循环引用错误修复
- 店铺图片不显示修复
- 商品视频上传功能添加
- marketing_campaigns 400 错误修复
- 商品情绪标签功能添加
- 情绪系统5个层次全部完成
- 探索页与犒赏铺隔离完成

### ⚠️ 需要手动操作的事项
1. 运行 `sql/add_mood_tags_to_campaigns.sql` 修复表结构
2. 创建 `videos` 存储桶（如果需要视频上传功能）

### 📝 待测试的功能
- 店铺图片是否显示正常
- 商品视频上传是否工作
- 营销活动页面是否还报 400 错误
- 情绪标签功能是否正常工作
- 探索页与犒赏铺隔离是否生效

---

## 下一步建议

### 优先级排序
1. **高优先级**：运行 SQL 脚本修复表结构
2. **中优先级**：测试所有修复的功能
3. **低优先级**：优化错误处理和用户提示

### 长期优化建议
1. 添加情绪化分享卡片
2. 添加情绪化搜索
3. 添加情绪化滤镜
4. 添加情绪化数据分析

---

**所有代码已完成并构建成功！现在请你运行SQL脚本，然后测试小程序，告诉我结果！** 🚀

如果有任何问题，随时截图发给我。

---

## 全面审查：管理后台 vs 用户端功能配对 + 协议合规化 (2026-07-06 下午)

### 审查范围
并行探查 admin-web（28个源文件）和 src/（136个文件），逐一对比功能配对和协议合规性。

### 发现的核心问题

#### P0 严重问题（4个）
1. **二维码函数名不匹配**: `api.ts` 调用 `'qrcodes'`，但 Supabase 部署的函数名是 `generate-qrcode` → 导致二维码全部 400 错误
2. **交易规则空引用**: 支付页引用《来店有喜交易规则》但该协议页面不存在
3. **提现协议缺失**: 提现页无任何规则说明（用户不知10%税、到账周期等）
4. **佣金规则缺失**: 推广中心无佣金规则协议页

#### P1 重要问题（8个）
- 段位规则、积分规则、商家入驻协议均缺失
- 设置页"关于"只列了用户协议+隐私政策，缺6个协议入口
- 小程序端admin缺用户管理/退款管理/公告管理3个页面
- admin-web商家后台4个页面（消息/优惠券/数据分析/广告）全是Mock数据
- 段位/佣金/积分规则全硬编码在代码中，无管理界面
- admin-web提现审核10%税率硬编码

### 功能配对矩阵
- 总后台8个功能：5个已配对，3个仅admin-web有（用户管理/退款管理/公告管理）
- 商家后台11个功能：7个已配对，4个admin-web是Mock，1个仅小程序有（营销活动）
- 用户端功能闭环：购物✅、提现✅、UGC✅，但退款⚠️(小程序端断链)、佣金/积分/段位⚠️(不可管理)

### 输出
- 详细报告：`docs/audit-report-2026-07-06.md`

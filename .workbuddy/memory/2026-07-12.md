# 2026-07-12 工作日志

## 01:30 商品/商家布局优化 — 构建与交互验证
- 背景：用户要"测试下，是否正常交互"。
- 构建：taro build --type weapp（不 rm .taro）→ BUILD_EXIT=0，无 error。
- 产物核验：StoreStrip 编译进 dist/common.js，首页/探索页通过 s.StoreStrip 引用；首页 FeedCard rounded-xl/110px/text-lg/calc(50%-4px)；探索页 gap-2/p-2/text-lg/110px/情绪区90px 全部编入。
- 交互测试（沙箱可运行）：react-test-renderer+act 在本 node 环境挂起（react18 scheduler 不推进事件循环，2分钟无输出）→ 改用 react-dom/server renderToStaticMarkup 渲染冒烟，[RENDER_SMOKE] PASS（含"精选好店"标题，无异常）。点击交互因无小程序GUI无法在沙箱真点，用源码审查+URL契约(与项目既有 store-home?id= 用法一致)确认。
- 沙箱限制确认：无微信开发者工具GUI，真机点击需用户在机器刷新验证。
- 验证报告已生成：INTERACTION_VERIFICATION.md。
- 临时测试目录 .tmp_smoke 已清理；react-test-renderer 装在隔离 managed workspace（C:\Users\zhanglin\.workbuddy\binaries\node\workspace），未污染项目 node_modules。

## 关键经验
- Taro 小程序"交互自动化测试"在沙箱的可行边界：renderToStaticMarkup 冒烟可跑；react-test-renderer+act 挂起不可用；真机点击必须用户自己点。
- esbuild alias 把 react/react-dom 指向 managed workspace 单实例，可零污染跑组件渲染测试。

## 02:00 商品/商家布局二次优化 —— 横向列表 + 统一小方图
- 用户反馈：1:1 全宽双列仍"图片太大、不美观"，要更统一方案。
- 诊断：双列里 1:1 方图宽度=卡片宽(~175px)→图霸屏；门店卡 140×84 横条也不方正。全站图片语言不统一。
- 方案(用户选定「横向列表」)：全站只用一种图 = 左边 80~88px 的 1:1 小方图。
- 首页 index/index.tsx：
  - FeedCard 由「双列大图卡」改「左88px方图+右信息(标题text-base/标签text-xs/价格text-lg)+右缘圆形加购」横向列表行；分享按钮缩 w-7；匹配徽标缩 text-xs。
  - 信息流容器由双列 calc(50%-4px) 两列 → 单列 flex-col gap-2。
  - 骨架同步为横向列表骨架(88px方图+3行)。
- 探索 explore/index.tsx：
  - 商品网格 grid-cols-2 → 单列横向列表行(88px方图+标题/情绪描述text-xs/标签text-xs/价格+加购)，去掉门店名行(留情绪描述与距离)，统一小方图。
  - 情绪推荐区(emotionProducts.slice(0,4)) grid-cols-2 → 同样横向列表。
  - ProductImagePlaceholder 改 w-full h-full 填满父容器(原固定120px高)，供无图时88px占位。
- 门店卡 store-strip/index.tsx：门店图 140×84 横条 → 1:1 方图(80×80)，卡宽 140→112，骨架图高 84→80。与商品列表小方图语言统一。
- 构建待核(K8B69q)。

- 构建已核(K8B69q)：BUILD_EXIT=0，产物确认 首页88px方图+无双列calc、探索无grid-cols-2+88px、门店卡112px/80px编入common.js无140px残留。方案文档已刷新为最终实现规格 REDESIGN_ELEME_LAYOUT.md。

## 09:47 管理后台启动失败排查
- 现象：用户截图浏览器访问 localhost:5177 报 ERR_CONNECTION_REFUSED，DevTools 还有跨域 iframe 加载错误。
- 诊断：admin-web Vite 开发服务器未启动；端口 5173/5177 均无进程监听。
- 处理：用 bash 直接执行 ./node_modules/.bin/vite（避免用 node.exe 执行 shell wrapper 报错），以 --port 5177 --host 启动，匹配用户原访问 URL。
- 结果：Vite v8.1.0 启动成功，Local: http://localhost:5177/，Network 也可达。admin-dev.log 保留在 admin-web 目录。

## 09:54 总后台「佣金兑付」提现列表参数梳理与一致性核查
- 已读源码：admin-web/src/pages/Withdrawals.tsx、admin-web/src/api/admin.ts、admin-web/src/types/index.ts、src/pages/withdraw/index.tsx、src/pages/admin-withdrawals/index.tsx、src/db/api.ts、supabase/migrations/00007+00077。
- 已梳理：列表字段、详情抽屉字段、状态流转、统计口径、脱敏规则、API 调用关系。
- 已生成报告：deliverables/总后台提现列表参数说明_前端一致性核查.md。
- 关键结论：admin-web 总后台与小程序用户端申请表单字段一致；核心差异在小程序 admin-withdrawals 审核入口采用「pending → paid」直接扣款，而 admin-web 是「pending → approved → paid」两阶段。建议收敛审核流程到 admin-web。

## 10:01 提现列表「姓名/手机号」合规诊断与修复
- 用户问"名字是否符合国家法规"，对照 PIPL(个人信息保护法)/反洗钱法/数据安全法做合规诊断。
- 诊断结论：收集真名+身份证号本身合法（反洗钱法 KYC + 代扣个税法定义务），但展示层有 4 处不合规：
  1. 列表页真实姓名未脱敏 → 修：新增 maskName() 函数，列表"申请人"列改为「张*林」格式
  2. 列表页手机号未脱敏 → 修：列表手机号列改为 maskPhone()「138****8888」
  3. 详情抽屉手机号未脱敏 → 修：详情手机号改为 maskPhone()
  4. 详情抽屉真实姓名保留明文（打款核对场景需要完整姓名，合规）
- 还存在 2 个需产品侧补的合规缺口（非纯代码可修）：
  - 收集身份证号缺 PIPL 第29条「单独同意」机制（需加勾选确认+告知弹窗）
  - 提现规则页缺 PIPL 第17/30条个人信息披露（目的/必要性/保留期限/用户权利）
- 构建：tsc -b --force + vite build 均 BUILD_EXIT=0。
- 文件：admin-web/src/pages/Withdrawals.tsx 新增 maskName/maskPhone 函数+3处调用。

## 10:10 总后台全量脱敏（方案1 落地）
- 用户确认"全部完成"，按方案1 全量开干。
- 新增公共工具 admin-web/src/utils/mask.ts：maskIdCard / maskName / maskPhone / maskAccount / maskAddress（身份证/姓名/手机/收款账号/地址五类脱敏）。
- Withdrawals.tsx：删除本地 4 个重复函数，改为 import 自 @/utils/mask。
- 总后台明文裸露页面全部接脱敏：
  - Members：列表+详情抽屉的 手机号、地址 → maskPhone/maskAddress
  - Orders：列表+详情抽屉 买家手机 → maskPhone
  - Users：列表 手机号 → maskPhone
  - Ledgers：points/emotion/gold 三标签 手机号 → maskPhone（replace_all）
  - EmotionClaims：列表手机、退款/封禁弹窗用户标识、封禁 confirm 框手机 → maskPhone
  - Merchants：列表 商家联系人电话 → maskPhone
- 未动商家端 merchant/*（履约需看买家电话/地址，保留明文；后续可选默认打码+点击查看）。
- 构建：tsc -b --force 退出0 + vite build ✓ built in 1.11s，无类型/编译错误（Tailwind content 警告与 chunk>500kB 警告为历史既有，与本改动无关）。
- 合规现状：展示层脱敏已全量闭环；仍缺 PIPL 第29条「敏感信息单独同意」勾选机制、第17/30条披露告知（产品侧补）。

## 11:25 协议页「不能点击打开查看」修复
- 用户报：用户协议、隐私协议点了打不开。
- 全仓 grep 协议跳转入口：PrivacyModal / settings / my-promotion / merchant-apply 跳转均正确；唯一坏点 = 登录页 login/index.tsx 第308-313行。
- 根因：登录页 `《用户服务协议》` 与 `《隐私政策》` 两行 Text 被包在勾选框的 `onClick={()=>setAgreed(!agreed)}` 内，自身无跳转，点击仅切换勾选态 → 打不开协议页。
- 修复：给这两个 Text 加独立 `onClick` + `e.stopPropagation()`，分别 navigateTo `/pages/user-agreement/index` 与 `/pages/privacy-policy/index`；stopPropagation 防止误触勾选。
- 注意：微信开发者工具此时仍开着（wechatdevtools 进程在），未跑 taro build（避免锁 dist/）；改动极小，用户 watch 模式应能热更，否则关工具后重建验证。

## 11:42 协议跳转修复构建验证（用户关工具后）
- 用户回"关闭了" → 确认 wechatdevtools 进程退出（NO_DEVTOOLS_RUNNING），WeChatAppEx 为聊天客户端不锁 dist/。
- dist 产物时间戳 11:42（前次构建实际已成功，任务记录被超时清理），grep dist/pages/login/index.js 含 `user-agreement`/`privacy-policy` 字符串（修复已编入）。
- 重跑 taro build --type weapp 做最终确认：`✓ built in 20.84s`，全部页面零报错。
- 结论：登录页协议跳转修复已生效，可重新打开微信开发者工具预览验证。

## 11:50 小程序全量 UI 体检（看不清/违规/不能点击）
- 扫描 src/pages 下 73 页面 + 组件，全量 grep 跳转/明文/字号/绝对化用语。
- 「不能点击」：跳转目标 100% 存在（含二级目录 merchant-campaigns/create、content-center/make、content-center/my-articles），无死链；唯一风险 = messages/index.tsx:91 动态跳转 target 来自后端 payload.page，catch 静默失败无 toast 提示。
- 「看不清」：🔴 merchant-products/index.tsx:694-695 硬编码 #CCC 落白底（对比度≈1.6:1 真看不清）；🟢 商家端大量硬编码 #999 + text-muted-foreground/50 + gray-400 偏弱但可见。
- 「违规」：🔴 小程序内置 admin-* 管理端明文展示敏感信息（admin-withdrawals:96 手机 / admin-refunds:131 手机 / admin-users:96 手机 / admin-merchants:90 商家电话）—— 上次只脱敏了网页版 admin-web，漏了小程序管理端；🟡「治愈」措辞在食养/情绪语境有疗效暗示风险（emotion-check 已用"传统食养参考"合规措辞可参照）；绝对化用语/医疗宣称未发现违规。
  - 报告已生成：UI_AUDIT_REPORT_2026-07-12.md（含修复优先级表）。
  - 待用户确认是否按优先级开修（2×🔴 + 2×🟡）。

## 11:53 继续：修复 UI 体检 2×🔴（小程序 admin 脱敏 + #CCC 灰字）
- 用户回"继续" → 确认开修 2×🔴。
- 新增 src/utils/mask.ts（与 admin-web/src/utils/mask.ts 同口径：maskPhone/maskIdCard/maskName/maskAccount/maskAddress），供小程序端复用。
- 小程序 admin-* 管理端接 maskPhone 脱敏：
  - admin-withdrawals/index.tsx:96 提现审核手机
  - admin-refunds/index.tsx:131 退款审核手机
  - admin-users/index.tsx:96 用户管理手机（顺带 toggleRole 确认弹窗里 u.phone 一并脱敏）
  - admin-merchants/index.tsx:90 商家联系电线
- merchant-products/index.tsx:694-695 视频说明两处 #CCC → #999（对比度达标）。
- 构建卡在微信开发者工具锁 dist/：后台 taro build 两次（woNkev / w2JVBg）均卡在编译阶段无输出；woNkev 中途 BUILD_EXIT=127（疑似之前 PowerShell 误杀进程干扰），w2JVBg 全新进程 3.5min 仍卡 = 微信工具锁目录。
- 待用户关闭微信开发者工具后，w2JVBg 将自动解锁继续完成，届时验证 dist/app.json 并汇报。

## 12:21 微信工具锁死循环确认（用户提供工具内报错）
- 用户粘贴微信开发者工具控制台报错：`[app.json 文件内容错误] app.json: 在项目根目录未找到 app.json (env: Windows,mp,2.01.2510290; lib: 3.16.2)`。
- 探明现状：w2JVBg 构建进程已自行退出（无残留），但 dist/ 仅半成品（pages/、vendors.js、taro.js 在，**无 app.json/app.js/app.wxss**）→ 编译写盘阶段被微信文件锁卡死、中途挂掉。
- 死锁本质：微信开发者工具开着 → 锁 dist/ → taro 写不进去 → 无 app.json → 微信反过来报"找不到 app.json"。代码改动本身无问题（taro/node 均在）。
- 唯一解法（沙箱无法杀用户进程）：用户**彻底退出微信开发者工具应用**（非关模拟器/最小化；Windows 任务栏右键→退出，或确认无 wechatdevtools 残留进程）→ 回复"关了" → 我 rm -rf .taro dist 后干净重建（约30s 出 app.json）→ 用户再打开微信工具预览。
- 2×🔴 代码修复已全部落盘（src/utils/mask.ts + 4 管理端页面脱敏 + merchant-products #CCC→#999），仅差最后一次构建验证。

## 12:24 构建解锁 + 发现 merchant-products 另有 2 处 #CCC 真问题（已补全修复）
- 微信开发者工具锁释放后重跑（xYXAFl / build4）：`✓ built in 25.25s`，dist/app.json(3695B)+app.js+app.wxss 全生成 → 死锁解除。
- 产物核验发现：build4 仅含 694-695 两处 #CCC 修复，但源码 merchant-products 仍藏 2 处 #CCC（原审计只报 694-695，漏报）：
  - :538 `placeholderStyle:#CCC` 输入框占位符白底看不清 → 改 #999
  - :855 "生成描述"按钮非激活态 `background:#CCC` + 文字 `color:#FFF` = **白字压浅灰、按钮文字看不见**（比视频说明更严重）→ 非激活底改 #E5E7EB、文字按状态 `#FFF`(激活)/`#6B7280`(禁用)，标准禁用态清晰可读
- 源码全仓 #CCC 复查：merchant-emotion-compile 还有 5 处 #CCC，但属标签芯片背景/边框兜底色 + 卡片描边（非白底文字），不在"看不清"🔴范围，未动（避免超范围改 bug）。
- 二次重建（Uo1P6i / build5）：`✓ built in 28.20s`，产物校验全过：
  - dist/app.json 合法（65 页）
  - merchant-products 产物 #CCC **清零**
  - #E5E7EB / #6B7280 均编入（按钮禁用态）
  - 4 管理端页面均含 maskPhone（脱敏生效）
- **关键经验（重要）**：微信开发者工具一旦重新打开就会再次锁住 dist/，导致下一次 taro build 卡死。工作流约定：预览时开微信工具即可（dist 已完整）；但若要让我再构建，必须先把微信工具彻底退出，否则必卡。

## 最终交付状态（截至 12:30）
- 小程序端 2×🔴 UI 体检问题全部修复并构建验证通过：① admin-* 管理端手机脱敏（PIPL 合规）② merchant-products 全部 #CCC 灰字/白字不可读问题（占位符+视频说明+生成按钮禁用态）。
- dist/ 为完整可用产物，用户可直接打开微信开发者工具预览。

## 15:00 确权流程改「支付成功自动发放」——订单中心/用户页/情绪账单清理跳转（Task #66~#69）
- 背景：用户要求"不用跳转了，用户是下了订单直接默认确权，不需要来回跳转"。
- 决策（AskUserQuestion）：确权时机=支付成功即自动发（未支付不发）；情绪选择=保留分享卡、自动预填提交（emotion-claim 页 autoSelectEmotions+autoTriggered 已满足）。
- Task #66 放宽 grantEmotionClaim 闸门：已落地（verified 判定纳入 pending_ship/pending_receive 等履约态，配送单也能过闸）。
- Task #67 支付成功自动发放：payment/index.tsx 纯金豆分支 + 微信/混合分支均注入 autoClaimAfterPay（跨门店按 C${parentOrderNo}${store_id前4位} 子单号逐店确权；best-effort 不阻断支付主流程）。
- Task #68 订单中心清理（src/pages/order-center/index.tsx）：删除「去确权」按钮、删除「标记已使用」按钮、删除收货后确权引导弹窗 claimModal、删除不再使用的 goClaim/handleMarkUsed/claimedSet/claimModal 状态与 getClaimedOrderNos 导入；handleConfirmReceipt 改为仅刷新列表+提示"已确认收货"。
- Task #69 用户页/情绪账单 CTA 改入口：
  - user/index.tsx:252 "去确权 ›" → "确权记录 ›"，跳转 /pages/emotion-bill/index。
  - emotion-bill/index.tsx：goClaim 重命名为 goShop（switchTab 到 /pages/index/index 逛逛），顶部 CTA "去确权 ›"→"去逛逛 ›"，会员权益 CTA "去确权提成长值 ›"→"去逛逛提成长值 ›"；对应 scss 注释同步。
  - 顺手更新 api.ts / payment / emotion-bill scss 中遗留的"去确权"注释。
- 代码扫描：全仓无残留 goClaim / 去确权 跳转调用；tsx 无重复 style= 属性（排除 JSX 卡死诱因）。
- Task #70 构建验证（待用户操作）：taro build --type weapp 后台跑 7 分钟卡死、无 dist/app.json；进程列表确认 **微信web开发者工具 又开着**（Program Files\Tencent\微信web开发者工具\node.exe 在跑）→ 锁 dist/ 导致卡死，已 kill 卡死构建（DYGfl9）。需用户**彻底退出微信开发者工具**后我才能干净重建。
- 结论：确权相关代码改动已全部落盘，仅差最后一次构建验证（被微信工具锁挡住，非代码问题）。

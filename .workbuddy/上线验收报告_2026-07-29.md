# 来电有喜 小程序 · 上线验收报告
> 审计时间：2026-07-29 19:59 ｜ 审计范围：全部今日已完成工作 + 上线硬门槛核查

## 一、验收结论

| 维度 | 结论 |
|---|---|
| 代码 / 功能层 | ✅ **达到上线标准** |
| 构建产物 | ✅ `dist/` 最新 19:57，核心入口齐全，小程序+网页后台均构建通过 |
| 平台审核层 | ⚠️ **需补充配置**（非代码，见第三节，必须在微信后台操作） |

---

## 二、已验证达标项（逐端实测）

1. **核心交易链路 · 无资损**（P0 已修）
   - 加购 → 购物车 → 结算 → 下单 临期折扣全闭环，`createOrderV2` 按 `batch_id` 从视图套 `effective_price` 实付
   - 立即购买路径、购物车路径价格一致，无"显示特惠价、实付原价"陷阱

2. **临期预警折扣 · 全端打通**
   - `expiry-engine` 已注册每日 `0 3 * * *` 定时调度（cron.job 查证 active=true）
   - 手动覆盖补写 `discount_stage` bug 修复
   - 全端同源读 `v_near_expiry_products`：C端临期频道 / 内嵌admin / 商家端 / 网页后台看板
   - 实测数据已流动：西瓜 ¥39.90 → 临期折后 ¥3.99（red / 90%off / 剩0天）

3. **首页定位地图**：杭州自动定位 + 附近门店推荐（腾讯地图 key 已接入）

4. **视觉 / 合规**：墨韵国潮主题统一；禁医疗宣称、附「不替代医嘱」

5. **商品详情**：生产日期 / 保质期展示（读 `stock_batches` 批次，与商家端同步）

6. **首页加购按钮**：修复图标缺失 + 透明背景双根因，赭红购物袋可点

7. **商品详情白屏**：`expiryEp/expiryBatch` 未声明 TDZ 已消除

8. **隐私合规**：`agreement/privacy-policy`、`user-agreement` 等协议页齐全

---

## 三、上线前 · 平台侧必办（微信公众平台后台，非代码）

> 以下任一项漏配，真机/审核都会失败。代码已就绪，但需你（或运营）在后台操作：

1. **request 合法域名白名单**
   - `https://pyqgsxcjmijtbstwthbn.supabase.co`
   - `https://apis.map.qq.com`
   - 注意：`project.config.json` 当前 `urlCheck:false` 是开发态，上线必须开启域名校验，否则真机请求被微信拦截。

2. **腾讯位置服务 Key 绑定**
   - 开发管理 → 腾讯位置服务，将 Key `D63BZ-GLHCL-XUIPF-EI7QI-6OK6S-7IB3Q` 绑定到 AppID `wxb5bdfdbb471a500f`

3. **用户隐私保护指引**
   - 设置隐私接口（getLocation / 用户信息），并关联 `privacy-policy` 页

4. **微信支付**
   - 确认商户号、支付目录、回调地址配置
   - ⚠️ 遗留：支付回调 `wechat-payment-callback` 仍向 `orders.wechat_transaction_id` 写（历史未修），上线前建议确认不影响对账

5. **类目资质**：食品类小程序需对应经营资质

---

## 四、非阻塞遗留（不影响发版，按需再做）

- 🟡 网页后台 `admin-web` 仍不能编辑/查看生产日期·保质期（方案 A 仅满足"与商家端同步"）
- 🟡 主浏览流（首页 feed / 搜索 / 普通详情）不挂"临期特惠"徽标（临期体验仅在频道内闭环）
- 🟡 `dist.bak.*` 历史备份未清理（占空间，非功能问题）

---

## 五、Final Verdict

**代码层面：可以上线。** 交易资损闭环、临期体系、定位、视觉合规、数据生成调度全部验证通过，无 P0/P1 阻断。

**真正发版前**：请完成第三节 5 项平台后台配置（尤其合法域名与位置 Key 绑定，否则真机直接白屏/定位失败）。

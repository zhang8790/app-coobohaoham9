# 保质期预警系统 · 全链路交付清单（2026-07-27）

> 范围：数据库迁移 → 后端引擎 → 前端三端 UI → 商品管理串联。所有代码已落地并通过构建，等待上线与提交。

## 一、系统分层与状态

### 1. 数据库层（Supabase，均已执行）
| 迁移 | 内容 | 状态 |
|---|---|---|
| `00213_expiry_engine_data.sql` | `v_near_expiry_products` 视图（含 qty 列）、`stock_batches` 批次表、`fn_daily_sales()` | ✅ 云端已执行 |
| `00214_expiry_engine_fix.sql` | 函数 / RLS / `system_config` 阈值配置 | ✅ 云端已执行 |
| `00215_expiry_decided_by_constraint.sql` | `decided_by` 约束扩展为 `('rule','ai','merchant_manual')` | ✅ 云端已执行 |

### 2. 后端引擎（Edge Function）
- `expiry-engine`：已部署，HTTP(anon) 触发验证通过 → `total:1, red:1, notified:1`
- 折扣封顶逻辑已修正（按可让利空间计算，避免 +330% 异常）

### 3. 前端三端 UI
| 端 | 入口文件 | 注册/路由 | 状态 |
|---|---|---|---|
| 小程序 C 端 · 临期特惠频道 | `src/pages/admin/admin-expiry/index.tsx` | `app.config.ts:64` | ✅ 构建产物齐全 |
| 商家手机端 · 预警管理页 | `src/pages/merchant/merchant-expiry/index.tsx` | `app.config.ts:30` | ✅ 构建产物齐全 |
| 商家手机端 · 中心页摘要卡+入口 | `src/pages/merchant/merchant-center/index.tsx` | NAV `bell-outline` 临期预警 | ✅ |
| 商家手机端 · 商品列表临期标签 | `src/pages/merchant/merchant-products/index.tsx` | 列表行内标签 | ✅ |
| 管理后台 · 全平台看板 | `admin-web/src/pages/Expiry.tsx` | `App.tsx:136` `/expiry` | ✅ |
| 管理后台 · 侧栏入口 | `admin-web/src/components/Layout.tsx` | 「临期预警」图标项 | ✅ |
| 管理后台 · 商品审阅临期标签 | `admin-web/src/pages/Products.tsx` | 列表行内可点击标签 | ✅ |

## 二、构建验证
- `admin-web` `npm run build` → **EXIT 0**（tsc + vite）
- 小程序 `bash build-weapp.sh` 冷重建 → **BUILD_EXIT 0**，
  `dist/pages/admin/admin-expiry`、`dist/pages/merchant/merchant-expiry`、`dist/pages/merchant/merchant-products` 产物均生成

## 三、待你侧操作（AI 无法代劳）
1. **微信开发者工具上传 `dist/`** → 小程序 C 端频道 + 商家端预警上线
2. **admin-web 部署环境重新 `npm run build` 部署** → 看板 + 商品标签上线
3. **git 提交/推送**（本次新增 6 文件 `??` + 修改若干，等「归档/推送」指令）

## 四、关键架构事实
- **保质期属批次维度**（`stock_batches.expire_at`），不在商品维度；商品表无保质期字段（符合设计）
- 商品管理页仅显示**按 `product_id` 聚合的临期状态标签**（正常/临期/紧迫/紧急），点击跳对应预警页，**零数据模型改动**
- `decided_by` 三态：`rule`（规则自动）/ `ai`（AI 决策）/ `merchant_manual`（商家手动覆盖），手机端与后台同写路径
